# worktree 4564 规范迁移前原件快照

此目录保存 `C:/Users/gzfangyue/.codex/worktrees/4564/angos` 在 2026-09-08 规范迁移前的 66 份原字节快照。

它独立于同名主目录快照：只用于审计或逐文件恢复，不能覆盖活动文件，也不能把旧流程重新视为当前要求。`snapshot/` 的相对路径保持原工作树结构；提交前已逐文件核验 66/66 SHA-256 一致。
