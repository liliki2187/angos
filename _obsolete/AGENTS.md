## Obsolete Content Rules

This directory is not part of the active project.

- Skip this directory by default.
- Do not use files here as implementation input or source of truth.
- Do not summarize, revive, or migrate content from here unless the user explicitly asks for historical lookup, recovery, or comparison.
- If files here disagree with active project code or docs, ignore the `_obsolete/` version.
