# Retired Full-Chain Demo Pages

This directory stores retired single-purpose pages removed from the active full-chain demo flow.

Current reason:

- `newspaper-fill-lab.html` was only a redirect shell into `world-mysteries-full-chain.html?paperlab=1`
- the active demo no longer exposes this extra entry point or packages it for release

Use this file only if you need to recover the old redirect behavior.
