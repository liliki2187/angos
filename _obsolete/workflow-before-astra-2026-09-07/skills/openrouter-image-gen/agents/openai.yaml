interface:
  display_name: "OpenRouter Image Gen"
  short_description: "Route transparent assets to OpenRouter and opaque assets to built-in imagegen."
  default_prompt: "Use $openrouter-image-gen to route transparent assets through OpenRouter GPT-5 Image and opaque assets through built-in imagegen, then save project-ready outputs under image_gen/."
