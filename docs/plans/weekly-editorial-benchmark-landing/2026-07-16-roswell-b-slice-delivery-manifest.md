# 罗斯威尔 1004 B 级报道图纵向切片交付清单

## 决策条

- **结论**：1004 单图、动态文章映射和默认双版运行预览已经通过增量工程检查，当前等待用户视觉确认。
- **影响**：左页下方第二个普通版位不再显示蓝灰占位块；1003 / 1004 都能在普通版位间跟稿显示，1005 / 1006 仍保持 fallback。
- **下一步**：用户先看默认双版截图中的小图语义、亮度和同刊一致性；通过后再单独推进 1005。

## 详情

- **一句话结论**：`article_id=1004` 已成为第二张可动态映射的 B 级普通报道图，但不据此声称整套普通报道资产完成。
- **实际完成**：内置真实生图、确定性中心裁切到 `756×696 RGB`、manifest v5、新文章查询、普通版位动态纹理切换、17 项针对性测试、Godot 导入与一张真实窗口截图。
- **当前没有工程阻断**：资源能加载，默认 feature-2 显示 1004；compact / tall 切换和未映射 fallback 均通过。
- **仍需用户裁决**：1004 与港口图并排时是否足够像同一期刊，复印机和反向纸页在小图中是否一眼成立，亮度是否仍低于主 / 副头版。
- **本轮不做**：不跑完整 UI / weekly smoke，不触发换稿、拖拽或确认全链，不生成 1005 / 1006，不新增采纳条目。

## 交付清单

- **交付对象**：`editorial_story_roswell_copy_refusal`，`article_id=1004`。
- **产物类型**：`runtime_state_preview`。
- **它能证明**：1004 在 `189×170` 默认普通版位中可显示；1003 / 1004 可按 `article_id + role` 解析并移动到 compact / tall；1005 / 1006 无映射时继续 fallback；纹理切换无旧图残留；图片层不消费鼠标输入。
- **它不能证明**：用户已采纳 1004；1005 / 1006 的风格方向；完整交互链、weekly smoke 或整套生产真源已经通过。
- **原始生图**：`image_gen/2026-07-16/20260716-weekly-editorial-roswell-copy-refusal-coarse-editorial-v1-original.png`。
- **运行资产**：`gd_project/Assets/ui/angus_packaging/weekly_editorial/assetized/editorial-story-roswell-copy-refusal-v1.png`，`756×696 RGB`。
- **运行截图**：`docs/screenshots/2026-07-16-weekly-editorial-roswell-b-slice/01-godot-two-standard-story-images-default.png`。
- **已过 gate**：Godot 新 PNG 导入成功；普通报道资产增量测试 `17/17`；截图为 `1920×1080` windowed OpenGL 真实运行画面并已肉眼复核。
- **未过 / 待确认 gate**：用户视觉确认；完整 UI / smoke / 42 项交互回归；1005 / 1006 扩产。
- **反向读法检查**：重点排除了“写实办公室 / 复印机产品渲染 / 3D 低多边形 / 白色灯箱 / 密集机械细节”；运行小图先读复印机、上卷拒绝纸与下方空白副本，未依赖按钮、标签或可读文字。
- **agent 例外**：按交接，本轮不是新 UI / 新布局，不重复 UI / UX 双 agent 链；按 clean-lowpoly-weekly 临时例外不自动调用旧像素坐标系美术守门。
- **文档 / 实现 drift**：STATUS 与 manifest v5 已同步；未改玩法规则或常量，因此不修改 GDD；用户尚未明确采纳 1004，因此不新增 A221。
- **允许下一步**：用户确认后只做 1005，或只返修 1004。
- **禁止跳到**：自动制作 1006、批量扩产、整套生产标杆 / 真源升格。

## 后续状态

- 2026-07-16 用户在审阅 1004 单图和默认双版截图后回复“继续”，已按 A222 记录为 1004 获确认并授权只进入 1005；本清单中“等待用户确认”的描述保留为交付当时状态。
