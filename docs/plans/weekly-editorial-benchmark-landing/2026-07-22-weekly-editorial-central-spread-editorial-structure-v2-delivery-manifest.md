# 发刊中央双版编辑结构稿 v2 交付清单

## 结论

中央双版黑白结构稿 v2 的右页普通报道空轨修正已落入 HTML：两张方图保持 `160×160`，由 `x=53` 移至 `x=26`，全高竖线改为短 folio 规则。原 UI Designer 与 UX 的全区域 PASS 仍保持撤回；待取得与当前代码一致的真实整版截图后再复核，不代表美术或 Godot 通过，也不得据此冻结。

## 用户审阅图

- `docs/screenshots/2026-07-22-weekly-editorial-central-spread-editorial-structure-v2/01-central-spread-editorial-structure-v2.png`
- 尺寸：`1040×920`。
- 只审：是否像正在排版的周刊、阅读顺序是否为主头版 → 副头版 → 普通报道、每篇图文是否属于同一编辑单元。

## 本轮改动

- 程序版 page header 的分区职责改译为 folio、栏目名、期号与页头基线，不恢复深色标题栏。
- 六个闭合卡片外框改译为开放式栏线、页面分隔线、共同边线与图注带。
- 主头版使用唯一 `28px` 两行大标题和最大横图；副头版改为横图＋右侧窄文字列，不再复制主头版。
- 左页普通位使用“标题 → 方图 → meta”；右页普通位使用“方图 → 标题 → meta”。四图全部为 `160×160` 正方形。
- 图片仅去饱和用于结构审阅；没有新增最终配色、纹理、按钮或状态。

## 自动与人工验证

- 浏览器视口 / 页面：`1040×920`，无滚动溢出。
- 六张真实报道图全部加载，natural size 非零。
- 六个标题的 `scrollWidth <= clientWidth` 且 `scrollHeight <= clientHeight`。
- 控制台 error / warning：`0`。
- UI Designer：右页普通报道 `PASS WITH CHANGES`；UX：右页普通报道 `FAIL / 无效空洞`。技术渲染检查仍通过，但区域审美 Gate 未通过。
- 正式 Godot、组件合同、GDD、manifest：未修改。

## 2026-07-22 用户复核修订

- 单个右页普通版位宽 `213px`，当前图片为 `[53,22,160,160]`；左侧空轨占 `24.9%`，仅承载竖线与两位 folio。
- 标题与 meta 从 `x=0` 起，图片却从 `x=53` 起，图文不能形成稳定共同轴线。
- 唯一修正保持图片 `160×160`，将其移至 `x=26`，把 folio 轨压缩为 `26px`，并把全高竖线缩为短页码规则；右页图片先行、左页标题先行的差异不变。
- 上述修正已写入 `index.html`。静态几何核查显示图片左右余量为 `26px / 27px`，标题与 meta 的既有矩形未改动。
- 本轮内置预览拒绝直接访问本机 `file://` 页面，因此旧截图不再作为当前代码证据；在补到真实整版截图前，浏览器渲染、控制台与视觉 Gate 均保持待验。

## 当前边界

- 取得修正后的真实整版截图并通过用户复核前，不进入美术包装，不从 v2 反向拆图，不启动左栏候选报道抽屉。
- 修正通过后，下一步是在修订后的 v2 排版骨架上恢复方案 1 的钴蓝、暖纸与目标印刷元素，并再次只交整张中央双版审阅。
