# 市政厅影子部门 1005 B 级报道图纵向切片路由卡

## 决策条

- **结论**：本轮只生产并接入 `article_id=1005` 一张普通版位报道图，继续复用既有 `article_id + role` 动态映射。
- **影响**：默认右页左下普通版位由橄榄 fallback 变为“实体市政楼＋不存在的影子侧翼”；不会改动双版布局、标题、换稿、拖拽或签批链。
- **下一步**：完成单图、manifest、定向测试和一张默认双版运行截图后，停在用户视觉复核，不自动开始 1006。

## 详情

- **明示需求**：用户确认 1004 后要求“继续”，按 A222 仅进入下一张 `1005`《市政厅新增了一个不存在的影子部门》。
- **必要补全**：新增一条 manifest 资产记录；既有通用查询已覆盖 1005，因此无需继续改运行时代码。
- **可延期工作**：1006 生图、完整交互矩阵、42 项 UI 审计、weekly smoke 和整套生产升格。
- **本轮建议**：以“建筑没有的影子侧翼和第二道门”作为唯一异常点，在 `189×174` / `189×170` 缩略尺寸确认语义，再接真实运行时。
- **停止条件**：1005 在默认 inner-1 显示，1003 / 1004 / 1005 动态映射通过，1006 fallback 通过，截图肉眼可读后停止。

## 路由

- **任务类型**：单一美术资产 / Godot 增量接入 / manifest / 定向测试。
- **风险等级**：risky，但交付阶段只到 `runtime_state_preview`。
- **目标载体**：Codex 内置 `image_gen`、Godot 双版运行时、`1920×1080` 桌面截图。
- **本轮必读真源**：两张 clean-lowpoly-weekly 标杆、支线风格规范、纸张与颜色合同、A220 / A222，以及 1003 / 1004 同刊图片。
- **agent 路由例外**：本轮不改变 UI、布局或交互，不重复 `ui_designer + ux_laoge`；clean-lowpoly-weekly 临时例外继续不自动调用旧像素坐标系 `angus_art_director`。
- **允许下一步**：用户确认后按同样单图策略制作 1006，或只返修 1005。
- **禁止跳到**：1006 自动生图、批量扩产、UI 重构、交互重写、整屏生产标杆升格。
