# 普通版位 B 级报道图纵向切片交付清单

## 决策条

- **结论**：工程与运行预览通过，视觉上等待用户确认；当前仍是一张普通报道的纵向切片，不是整套资源完成。
- **影响**：可以据此判断港口图是否值得作为后续 B 级报道图的构图尺度；在用户确认前不批量扩产。
- **下一步**：用户复核默认、局部换稿和拖拽三态；通过则按同一合同做下一张 B 图，不通过则只修这一张。

## 详情

- **一句话结论**：`article_id=1003` 已从纯色色块升级为真实无字报道图，并能在四个普通版位之间随文章移动。
- **我实际做了什么**：真实生图一张并做一次保持构图的明度返修，中心等比裁切为 `756×696` 母图；新增 manifest v4 条目、Godot 内容层、fallback 互斥、针对性测试和三态截图。
- **现在卡在哪里**：无代码阻断；需要用户判断亮度、构图与双版气氛是否成立。
- **为什么不能跳过**：单图的亮度和符号密度会直接决定后续普通报道资源是否统一；未确认前批量生成会扩大偏差。
- **下一步怎么验证**：先看 `01` 默认截图中的主 / 副 / 普通三级层级，再看 `02/03` 是否仍只有局部按钮和描边反馈。
- **本轮不要做**：不把这张图升格为整套美术真源，不声称主 / 副头版对 1003 的大图裁切已覆盖。

## 交付清单

- **交付对象**：普通版位 B 级报道图 `editorial_story_harbor_tomorrow_tide`
- **产物类型**：`runtime_state_preview`
- **风险等级**：risky
- **它能证明**：189×170 / 189×174 下“广播喇叭＋异常潮汐”可读；1003 在四个普通版位之间移动时图片随文章更新；无映射报道使用 fallback；图片不消费输入；原交互状态保持。
- **它不能证明**：其它 B / C 资源的风格一致性；整套杂志已经完成；1003 进入主 / 副头版时的大图适配；用户已经接受当前亮度和构图。
- **对照真源 / 标杆**：`benchmark-board-01.png`、`benchmark-board-02.png`、clean-lowpoly 支线风格规范、paper / color / page contracts、compact / tall slot contracts。
- **截图 / 文件路径**：`docs/screenshots/2026-07-15-weekly-editorial-standard-story-b-slice/`、`gd_project/Assets/ui/angus_packaging/weekly_editorial/assetized/editorial-story-harbor-tomorrow-tide-v1.png`。
- **几何 / 正交检查**：报道图本身不承载可写 UI 面，不宣称美术壳正交；运行 rect 由自动测试锁定为 compact `189×170`、tall `189×174`，采用中心 cover。
- **已过 gate**：新资产审计 `12/12`；既有双版交互审计 `42/42`；标题自适应 `8/8`；Godot agent smoke 通过；三张 `1920×1080` windowed OpenGL 截图已人工查看；首版过亮被交付前检查拦截并以 v2 明度返修替换。
- **未过 / 待确认 gate**：用户视觉确认；后续多图的一致性复审；主 / 副头版对普通报道图的通用映射。
- **反向读法检查**：亮色港口图可能被读成第三头版；首版平均灰度 `135.6` 已被拦截，v2 降至 `81.1` 并取消近白天空。当前运行截图中其面积、饱和度和位置仍低于两张头版，但最终观感由用户裁决。
- **已调用 agent / 复审**：UI Designer 给出构图、安全区与配色；UX 老哥条件放行并要求图片忽略输入、资源按文章 ID 更新。
- **未调用 agent 与原因**：clean low-poly weekly 临时例外不调用旧像素方向的美术守门；无玩法 / 数值 / 架构变更。
- **文档 / 实现 drift 检查**：manifest 已升 v4，A218 与 STATUS 同步；标题、页面和交互合同未变。
- **下一步允许做**：用户确认后继续下一张 B 级报道图，或只返修当前单图。
- **下一步禁止跳到**：批量 B / C 图、整套生产真源、未经确认的全局视觉升格。
