# 普通报道图阶段标签冒充整屏收口 Loop Log

## 决策条

- **结论**：1003–1006 只完成了“报道图资产在程序化结构宿主中的运行验证”；先前写成“编辑器侧收口”属于阶段标签过高，当前整屏不是最终效果。
- **影响**：用户确认、资产映射、交互和容量态证据仍有效，但只能证明局部资产与结构运行；整屏视觉包装必须标记为未开始。
- **下一步**：先做现有发刊编辑界面的 UX 诊断与桌面 16:9 包装方案；旧 weekly layout smoke 同步降为独立工程清理。

## 触发与原因

- **触发来源**：工作流 closeout gate 在完整周流程 smoke 中失败。
- **表面问题**：`test_phase_flow.gd` 与 `test_settlement_result.gd` 通过，但 `test_weekly_run_layout.gd` 报 22 项失败。
- **失败集中区**：world map 稳定尺寸、INDEX 标题 / 副标题安全区、Region detail 边距、CTA 提示 / 操作边距、锁定地区对应状态、地区节点图可见性，以及已漂移的 `EditorialPhase/.../StatsPanel/StatsVBox/StatsScroll` / `Body` 节点路径。
- **直接原因**：同一 weekly smoke 同时覆盖发刊编辑器、WMW 与旧布局断言；当前运行时与部分布局断言 / 节点路径存在漂移，而本轮改动范围仅为普通报道图资产状态与证据收口。
- **附加执行异常**：最终复跑 42 项交互审计时，受限沙箱内 Godot 连续两次发生原生 `signal 11`；沙箱外同命令复跑退出码为 `0`，机器审计保持 `42/42`。因此保留为环境稳定性证据，不把崩溃轮伪装成通过，也不把成功重试误当成布局 smoke 已恢复。

## 用户纠偏（2026-07-17）

- **用户看到的症状**：整屏仍明显是粗糙的程序生成三栏结构，只在中央杂志和版位里加入了部分报刊资产；顶部状态、候选池、右侧复核、按钮和外壳没有完成包装。
- **AI 错误判断口径**：把普通报道图的 `25/25`、结构交互 `42/42` 和四容量态截图合并成“编辑器侧收口”，用局部资产与运行 gate 替代整屏视觉包装 gate。
- **证据复核**：`WeeklyRunEditorialPhase.gd` 仍以 `PanelContainer`、`StyleBoxFlat` 和运行时 Label / Button 构造顶部状态条、候选池、工作区外壳与发刊复核；真实位图主要集中在主 / 副版纸面壳与报道图。六版位截图第一眼也仍读成程序结构稿。
- **正确阶段标签**：`runtime_state_preview / unpackaged_programmatic_structure_host`。
- **禁止误读**：不得称为最终界面、整屏包装完成、编辑器侧收口、生产 UI 或真源候选。

## 分类与处理

- **错题家族判断**：这是 F1“验证等级冒充”的实际复发；局部图片与结构运行通过被误标为整屏编辑器收口，F1 已知复发计数更新为 `≥19`，最后复发日期更新为 2026-07-17。
- **当轮处理**：保留普通报道图资产、结构交互和容量态各自的 PASS；将整屏视觉包装改为 NOT STARTED；manifest、STATUS、Router Card 与交付清单全部降级，不再把测试漂移当作唯一阻断。
- **防复发保护**：发刊界面后续必须分开列出 `story_asset_slice`、`structure_runtime`、`visual_packaging`、`weekly_smoke` 四层 gate。只有外壳、候选池、复核区、CTA、文字系统、状态矩阵、UI / UX 复审与真实整屏截图均通过，才允许使用“界面收口 / 最终 UI”。
- **沉淀判断**：现有 F1 家族足以承载，不新增全局硬规则；更新 F1 复发计数，并在本资产线 STATUS、交付清单与 Loop Log 中写明正确阶段。
