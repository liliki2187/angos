# 罗斯威尔 1004 B 级报道图纵向切片路由卡

## 决策条

- **结论**：本轮只生产并接入 `article_id=1004` 一张普通版位报道图，同时把普通版位从固定 1003 改为按文章动态映射。
- **影响**：可以验证 1003 / 1004 两张普通报道图的同刊一致性、跟稿移动与 fallback 互斥；不会改动双版布局、标题、换稿、拖拽或签批链。
- **下一步**：完成单图、manifest、针对性测试和一张默认双版运行截图后，停在用户视觉复核，不自动开始 1005 / 1006。

## 详情

- **明示需求**：替换左页下方第二个普通版位的蓝灰 fallback，题材为“复印机＋拒绝被复制的纸”。
- **必要补全**：新增 `article_id + role` 的 manifest 查询，让 1003 / 1004 在 compact / tall 间各自跟随文章；未映射文章必须清空旧纹理并回到原 fallback。
- **可延期工作**：1005 / 1006 生图、完整交互矩阵、42 项 UI 审计、weekly smoke 和整套生产升格。
- **本轮建议**：先在原图与 `189×170` 比例下确认主剪影，再接运行时；只跑一项资产测试和一张默认双版截图。
- **停止条件**：1004 在默认 feature-2 显示，1003 / 1004 动态映射通过，1005 / 1006 fallback 通过，截图肉眼可读后停止。

## 路由

- **任务类型**：单一美术资产 / Godot 增量接入 / manifest / 针对性测试。
- **风险等级**：risky，但交付阶段只到 `runtime_state_preview`。
- **目标载体**：Codex 内置 `image_gen`、Godot 双版运行时、`1920×1080` 桌面截图。
- **本轮必读真源**：两张 clean-lowpoly-weekly 标杆、支线风格规范、A220、当前漂移 Loop Log、现有三张同刊图片与默认运行截图。
- **agent 路由例外**：本轮不改变 UI、布局或交互，按交接明确要求不重复 `ui_designer + ux_laoge`；clean-lowpoly-weekly 临时例外继续不自动调用 `angus_art_director`。
- **允许下一步**：用户确认后按同样单图策略制作 1005，或只返修 1004。
- **禁止跳到**：1005 / 1006 批量生图、UI 重构、交互重写、整屏生产标杆升格。
