# 普通报道图资产序列验证 Router Card

## 路由结论

- **任务目标**：在用户确认 1006 后，验证 1003–1006 普通报道图能否在当前程序化结构宿主中稳定运行，并诚实标记产物阶段与全局周循环回归状态。
- **任务级别**：normal；包含可见运行态截图和跨状态回归，但不新增 UI、布局、玩法或图片资产。
- **本轮边界**：只更新采纳记录、manifest 状态、交互 / 资产 / 容量态 / 周流程证据与交付文档。
- **明确不做**：不生产新的 B / C 级资产，不修 WMW / 地区详情，不擅自改旧布局测试，不设计或落地整屏视觉包装，不升格最终 UI / 整屏生产真源。

## 路由与证据

- 用户确认：A225。
- 普通报道图定向审计：`25/25`。
- 发刊编辑器交互回归：`42/42`。
- 容量态：真实 Godot `1920×1080` 窗口下 `0/6、3/6、5/6、6/6` 四态。
- 全局周流程：分别运行 phase flow、settlement result、weekly layout，禁止把局部通过折叠成“weekly smoke 通过”。

## 当前判定

- 普通报道图资产在当前结构宿主中的运行验证：通过。
- 整屏视觉包装：未开始；当前截图不能作为最终界面效果。
- 全局 weekly layout smoke：测试漂移待同步；现行 WMW / 地区任务板替代结构测试 `86/86` 通过。
- 允许继续：先按改进现有 UI 的流程做 UX 诊断，再由 UI Designer 输出桌面 16:9 包装稿与资产化边界；旧 smoke 同步作为独立工程清理。
- 禁止跳到：未经裁决自动开始专题图、内页图、地图、波形或证据模块。
