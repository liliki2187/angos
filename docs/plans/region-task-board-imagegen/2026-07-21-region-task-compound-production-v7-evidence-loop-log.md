# 区域任务 compound v7 运行证据文字叠压 Loop Log

## 结论

v7 首次真实整页截图中，短签 compound 本身通过，但右 dossier 的第三行风险依据与底部建议发生叠压。该截图未进入用户交付；风险区已在原位置内扩容并冻结为“风险等级 + 风险依据 + 建议”三层，复拍后文字互不覆盖。

## 错误表现

- 原风险面板高 `150px`，风险正文只有 `58px`；加入显式风险等级后，`等级 + 截稿 + 风险依据` 在真实中文行高下需要三行。
- 建议仍固定在局部 `y=108`，导致第三行正文与建议基线接近，在 1920×1080 证据图中明显叠压。
- 单元测试只验证字符串存在，没有覆盖文字区域的实际行高压力，因此逻辑通过不能替代截图 Gate。

## 修正

- 风险面板从 `372×150` 调整为 `372×174`，仍位于 dossier 原有信息区与 CTA 之间，不移动主 CTA。
- 风险正文改为 `342×76`、最多三行；建议改为 `342×34`、最多两行，并保留省略策略。
- `dossier_contract.json` 新增 `region_node_risk_level` 与 `region_node_recommendation`，明确截稿 / 链条只能作为依据，不能替代风险等级。
- 重新导出 `02-right-edge-dossier-v7.png`，确认风险等级、两条依据与建议均可读且不覆盖。

## 后续防错 Gate

凡 dossier 新增字段或中文行数变化，除字符串测试外必须同时提供选中态真实 1920×1080 截图；至少覆盖风险等级、两条风险依据和一条建议的组合压力。发现叠压时先修正信息区容量，不通过缩字、删字段或遮挡掩盖。

## 第二次内部复核：fixture 同源性

UX 终审进一步发现，右边缘证据曾把地图 pin 改成连续任务与 `1天`，但 fixture 的 dossier 仍保留限时任务、`2天` 和截稿字段。该问题只存在于截图 fixture，不是 production payload；但它会让证据无法证明同源绑定，因此同样阻断交付。

已执行：

- 右边缘 fixture 的标题、类型、耗时、风险依据与建议统一由同一 chain task 设置，复拍图中均为“追踪 / 1天 / 连续追踪”；
- 五点密集 selected fixture 的 dossier meta 改为读取 selected node 的真实 kind 与 days；
- production integration 增加 `m330` 同源断言：标题匹配、meta 含“追踪 / 2天”、风险依据含“连续追踪”且不得残留“截稿”、建议存在；Godot 4.6.3 全部通过；
- 补充 cluster 收拢与展开真实截图，关闭 UX 的证据 P2。

后续截图 fixture 只要改变 pin 的 kind / days / state，就必须同步从同一 node 派生 dossier 字段；不得用“视觉示意值”拼接出跨对象证据。
