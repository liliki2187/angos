# 区域任务台外围 UI 局部通过、整屏失败 Loop Log

## 结论

用户明确指出：除地图外，左侧、右侧与下方组件仍然粗糙，与程序生成界面没有区别，文字和界面结合也不好。该判断成立。此前 dossier v1 的 UI / UX / 美术 `GO` 只能证明局部可读、拆件成立和状态无跳位，不能外推为整屏视觉通过。

## 错误链

```text
先按组件切资产
    → 只验证无字外壳、alpha、尺寸、文字安全区和状态
    → 旧 Panel / Button / Label 骨架继续拥有整屏构图
    → 美术只变成局部表皮，文字仍按程序控件盒排版
    → 每件局部都不越界，但左 / 右 / 下互不成体系
    → 地图像正式美术，外围像三组开发面板
```

## 本次 false pass

- 评审主证据集中在 `412×960` dossier 局部和单组件 1×，没有把 `1920×1080` 整屏的完成度落差作为最高 Gate。
- Gate 过度强调尺寸、alpha、NinePatch、文字不越界与状态可读，缺少“文字是否被印进版式”“三侧是否共享同一纸材 / 轮廓 / 色块 / 基线”“25% 缩略图是否仍像同一编辑工作台”的判断。
- 运行截图中仍出现“推进一天·待接入”“时间推进规则尚未接入”“本轮只验证”等开发交接语言，却没有被当作整屏游戏感 P1 拦截。

## 当前裁决

- 中央地图与 pin v7 继续冻结。
- dossier v1 降级为 `technical_assetization_evidence_only`，不冻结为整屏视觉真源。
- 左栏原生 Panel / Button 外观、dossier 内部程序排版、底栏三列程序布局与顶部 HUD chrome 均不再视为视觉冻结。
- event card 的数据、命中、滚动与外框合同继续冻结；其可见美术外观在用户授权下一轮视觉重做前不修改。

## 防错 Gate

下一轮只先做一张 `1920×1080`、单方向、带真实中文的整屏 selected-ready visual target。地图与 pin 原样保留；左索引、右 dossier 内部、底部行动 / 日程带和顶部低权重频道条必须在同一张稿中建立共享的纸材、色块、切角、轮廓线重与四级文字系统。

整屏稿通过前：不继续生独立 shell、section、CTA 或任务卡，不拆 atlas，不修改 Godot 正式实现。整屏稿通过后再反推资产切分与必要的合同升版。

