# WMW A263 v0.3 第三卡 footer 双审

## 结论

`PASS｜UX P0/P1/P2=0｜UI P0/P1/P2=0`。v0.2 的第三卡状态签裁切 P1 已关闭；v0.3 恢复为等待用户完整整屏视觉确认的 `1920×1080 TARGET ONLY` 候选。

## 父级与机器证据

- source crop `848×120`、target `367×52`，比例误差 `0.13%`，非均匀缩放误差 `0.127%`。
- protected 保留率 `100%`，四向 crop loss 均为 `0`，左右端帽完整。
- 最终右 / 上 / 下边距 `8.22 / 8.67 / 5.2px`；标题到签体 `99.25px`。
- “锁定”文字相对实际可见签体中心误差 `(0.49,2.77px)`。
- v0.3 相对 v0.2 的变化只位于第三卡 footer `[49,744,367,52]`；默认 / 确认差分仍只在日程区。
- 日程日期 / 动作仍为 9px；右栏正文 / 标题 / 首任务仍为 10px / 10px。

## UX 老哥

`PASS / P0=0 / P1=0 / P2=0`。状态签不再读成被截断按钮；第三签约 `113×41`，第二签约 `101×46`，虽略宽矮，但右对齐、同色、同字级且标题至签左缘约 99px，不构成标题侵占、坏控件或新层级冲突。日程和右栏没有回退。

## UI Designer

`PASS / P0=0 / P1=0 / P2=0`。source/target 比例、direct resize、protected 保留率、端帽、边距、实际色域文字中心和 v0.2 区外不变全部通过。第三签尺寸微差在较矮第三卡中保持视觉平衡，不构成组件错型。

## 边界

该 PASS 只覆盖当前 1920×1080 整屏目标稿和双态流程。Godot、runtime、atlas、正式合同、低分辨率清晰度、hover / pressed / locked 动态状态均未进入本轮。

