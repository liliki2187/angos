# WMW v5.1 资产化 Bridge Delivery Manifest

## 决策条

- **结论**：资产化 bridge 已通过 UI → UX → 父级合并，可作为日程器纵向切片输入。
- **影响**：明确哪些资产可复用、哪些必须新建、哪些必须保持运行时；A237 与现有正式合同不变。
- **下一步**：建立 `schedule_gate` 派生候选规格，再制作一张无字日程器母版；不生成有色整屏。

## Manifest

- **产物类型**：`assetization_bridge_brief`。
- **风险等级**：normal。
- **交付文件**：`2026-07-20-world-map-wmw-v5-1-assetization-bridge-brief.md`、Router Card、本复审和本 Manifest。
- **它能证明**：v5.1 每个区块的资产 / runtime 分层已定义；日程器为第一切片；B2.12、CTA、照片和 A5.1-H 的复用边界已收敛；发行审计隔离有明确 Gate。
- **它不能证明**：任何新有色资产已生成、日程器视觉通过、advance_day 已实现、A5.1-H 合同已建立、Godot 已装配。
- **UI Designer**：完成逐区映射、z-order、安全区、色彩边界、切片选择和生产顺序。
- **UX 老哥**：首轮 `P0=0 / P1=4 / P2=1`；修订后仅剩 disclosure 内部矛盾，父级按 A211 / A229 统一为等高 collapsed / expanded；最终回归确认 PASS，可冻结为下一阶段输入。
- **父级直接标杆复审**：已查看 benchmark-board-01/02；确认深色板、现代周刊纸件、大块低多边形、正交功能面和克制 olive / teal / rust 方向，不允许旧像素、重半调、泛黄档案或 GIS 化。
- **美术指导路由**：clean-low-poly weekly 临时例外，不自动调用 `angus_art_director`。
- **未改范围**：PNG、Godot、正式合同、compact A5.1、A237 几何与文案。

## Gate

- A237 几何保护：PASS。
- 逐区复用 / 新建 / runtime / 禁烘焙：PASS。
- 日程状态矩阵与 0 天边界：PASS。
- B2.12 照片 / 状态解耦：PASS。
- A5.1-H disclosure 与 CTA 锚定：PASS。
- `locked_disabled`：PASS（brief 要求，资产尚未生产）。
- Z90 发行零节点 / 零像素：PASS（brief Gate，尚未跑发行构建）。
- 有色稿 / Godot / 正式合同：NOT STARTED。
