# WMW 整屏功能真值 v5.1 复审

## 结论

`black-white-full-map-v5-1-default.png` 与 `black-white-full-map-v5-1-schedule-confirming.png` 已生成并通过几何审计、父级原图目检和 UX 老哥回归。用户于 2026-07-20 明确回复“没有什么问题，推进”，当前判定为 **PASS（structure wireframe / TARGET_ONLY）**，并冻结为当前整屏黑白布局真源。

本轮保持用户已采纳的 A233 / B：A5.1-H warning 派生态不显示“红线升温”，且没有产生独立信息缺口。地区级存在性由任务摘要承担，具体截止由任务行承担。

## 真实产物

- 默认态：`docs/prototypes/world-map-wmw-black-white-structure/black-white-full-map-v5-1-default.png`
- 日程确认态：`docs/prototypes/world-map-wmw-black-white-structure/black-white-full-map-v5-1-schedule-confirming.png`
- 几何审计：`docs/prototypes/world-map-wmw-black-white-structure/black-white-full-map-v5-1-audit.json`
- 可复现脚本：`docs/prototypes/world-map-wmw-black-white-structure/render_full_map_functional_truth_v5_1.py`

两张 PNG 均为真实 1920×1080 黑白结构图，不是 Godot 运行截图。

## 相对 v5 的最小修正

- 审计层改为 `LOW-FIDELITY V5.1 · TARGET ONLY · 1920×1080`，撤下没有独立真源的 `ISSUE 001`，只保留 `WEEK 01`。
- 初始样例按 `Week 1 / Day 1 / reputation 45 / roswell_dossier=false` 表达：东亚与太平洋均锁定；北美保持选中可用。
- 删除没有 edge / adjacency 数据的三条地图闭环虚线，不新增路线、图例或筛选器。
- 左下 342×246 日程器保留原几何，补齐默认态与原位二次确认态；不再宣称红线或深链随天推进，剩余天数归零进入编辑部阶段。
- 右栏任务摘要候选改为 `常驻 2 · 限时 1 · 深链 1`；四行 meta 使用正式内容类型 `科学纪实 / 大众热度 / 神秘玄学`，右标签表达 `常驻 / 限时至第 4 天 / 深链`。
- 删除玩家正文中的设计注释，第二行改为候选玩家文案 `军方巡逻、档案残页与异常雷达同时露头。`
- UX 回归建议把默认态 `当前到期：无任务到期` 压缩为 `当前：无任务到期`，已在最终图中执行。

## 两态文案

### 默认态

- `当前第 1 天 / 剩余 7 天`
- `推进到下一天`
- `点击后查看推进影响`
- `当前：无任务到期`
- `日程归零：进入编辑部阶段`

### 日程确认态

- `确认推进到第 2 天`
- `再次点击执行 · 后果见下方`
- `确认后：剩余 6 天 · 无任务到期`
- `限时任务：雷达异常仍开放至第 4 天`

## 几何与像素审计

- 画板：`1920×1080`。
- 三栏、地图 `[426,96,924,936]`、日程器 `[36,810,342,246]`、右栏 `[1398,24,480,1032]`、CTA `[1425,957,426,75]` 均保持。
- 路线删除使用三条真实 path 的精确差异掩膜，再膨胀 6px 作为允许区；精确变化 2142px，允许 mask 21438px。
- 默认态相对 v5：`outside_allowed_pixels = 0`。
- 确认态相对 v5：`outside_allowed_pixels = 0`。
- 默认态与确认态的差异 bbox 为 `[52,907,332,1039]`，`outside_schedule_pixels = 0`。
- 大陆轮廓、网格、pin 坐标、三栏框体、任务行盒体和 CTA 未漂移。

## UX 回归

UX 老哥最终判定：**P0=0、P1=0、P2=2，PASS（TARGET_ONLY）**。

- 删除“红线升温”后无信息缺口；`限时 1 → 限时至第 4 天`形成摘要—明细路径。
- 日程默认 / 确认文案不再伪装深链随自然日推进，Day 1 / 7 天到 Day 2 / 6 天的预览成立。
- 东亚与太平洋在当前 fixture 下锁定，左卡与地图 pin 一致。
- `常驻 2 + 限时 1 + 深链 1 = 已展开 4/4`，四任务行数量与类型一致。
- 删除路线没有破坏地区选择地图，反而撤下了不存在的移动路径暗示。
- 默认态与确认态通过图标、主动作、次说明和后果四层变化形成足够区分。

两个 P2 中，重复的“当前到期”已修；剩余一项是栏目类型与任务结构类型未显式标轴。当前不新增模块，也不直接加“构成：”，只在后续观察测试确有误读时再处理。

## 用户最终裁决

- 接受摘要 `常驻 2 · 限时 1 · 深链 1`。
- 接受正文第二行 `军方巡逻、档案残页与异常雷达同时露头。`
- 接受 v5.1 默认 / 日程确认两态整体视觉，不要求继续修改。

以上登记为 A237；两处文案与当前整屏黑白布局一并冻结。

## 边界

- 没有修改 Godot、正式组件合同或 compact A5.1 frozen。
- 没有生成有色稿或生产美术。
- `advance_day`、A5.1-H 全高展开和世界地图截止预览仍未接入 runtime。
- 当前图只能证明目标结构和静态状态文案通过，不能证明点击、扣天、刷新和锁区反馈已经运行。
- 下一阶段先做资产化 / 有色映射 brief；不得把“允许推进”解释为可跳过分层与 gate 直接生成有色整屏。

## 冷备 agent 提醒

本任务仍命中冷备 `game_logic_check` 的解冻条件：进入 runtime 前需要核对时间推进、地区解锁与任务生命周期的跨系统边界。若不解冻，替代方案是继续以专项真值表、状态测试和真实交互截图逐项放行。
