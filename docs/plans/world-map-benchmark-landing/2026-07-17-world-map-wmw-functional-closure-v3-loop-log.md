# WMW 空容器改成空背景 Loop Log

## 三行决策条

- **结论**：功能密度 v2 未解决左右留白，只把空白从组件内部搬到组件外部，结构稿作废。
- **影响**：182px 日程器、62px 浅袋口、右下无框背景与对应“呼吸区”判断全部撤回；紧凑双回执与扩大地图保留。
- **下一步**：左栏恢复完整日程职责，右栏建立 A5.1-H 全高派生结构；本轮只交新的黑白结构稿，用户确认后再决定合同与生图。

## 触发

2026-07-17 用户查看 v2 结构稿后指出：左下和右下仍然有大片空白，结构显然不合理。

## 错误表现

1. 左下日程器从 246px 压至 182px，但释放出的 64px 没有归属任何真实职责。
2. 右侧把 231px 重型文件袋缩成 62px 浅袋口，下面留下 169px 无框世界背景。
3. 结构稿用 `UNFRAMED BREATH`、`WORLD BACKGROUND`、`NO TEXT / NO HIT` 主动标注这些空区，等于承认栏位没有完成。
4. 把“删除过大容器”错误等同于“完成整栏收口”，没有再次检查最后一个真实职责到栏底的距离。

## 根因

- 信息价值 / 容器体量审计只检查了大卡和大装饰是否过重，没有继续检查被释放面积由谁接管。
- 把无边框背景当成天然安全答案，忽略三栏整屏宿主中有界栏位仍需要职责落底。
- 过度保护紧凑 A5.1 的 frozen 高度，没有及时建立继承语义但独立定高的全高变体。

## 修正

- 恢复 342×246 日程器并用现有推进链完整填充。
- 保留 924×822 地图与双 450×90 回执。
- 建立 A5.1-H 全高派生结构，不修改原 A5.1；使用既有四条可见任务填充任务情报区，CTA 底锚定。
- expanded 与 collapsed_summary 等高，禁止收起后制造新空洞。

## 防复发

- `workflow-gates.yml` 新增 `component_removal_exposes_equivalent_unowned_void_in_bounded_column`。
- workflow harness 新增栏位职责闭合审计。
- F1 已知复发由 `≥22` 更新为 `≥23`。
- UX casebook 已补充“空容器改成空背景”的同日复发。
