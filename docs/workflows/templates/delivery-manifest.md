# 交付记录（复杂交接或生产候选按需使用）

不要求每轮新建；可将以下内容写入现有状态页。填实际发生的事，不为填表补做无关阶段。

- 产物类型：
- 变更范围：
- 参考与当前决定：
- 交付文件：
- 验证证据：
- 未验证边界：
- 未解决风险／需要决定的事项：
- 下一步（仅在需要时）：

生图、合成与运行证据应分别标识。复用旧证据时写明版本和为何仍适用。子代理意见与量化结果可链接，不默认整份粘贴。画布等侧车如有采纳，仅记录对应仓库真源是否已同步。

`scripts/check_delivery_manifest.py` 只检查记录完整性，不能证明视觉、运行或用户认可。
