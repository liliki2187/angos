# 会话交接 · 当前状态（给新 Cursor 对话用）

> **用法**：新开对话时，把本文件整篇 `@` 引用，或把文末「一键开场」复制粘贴即可。
> **工作区**：`d:\angos`（或 clone 同结构仓库）
>
> **文档修订**：2026-05-14
> （说明：历史上曾标注 `2026-03-26` / 磁盘 `LastWriteTime 2026-04-17`；本版已按仓库当前结构与路径整体更新。）

---

## 一、Git 现状（必读）

| 远程       | URL                                      | 当前怎么用 |
|------------|-------------------------------------------|------------|
| `origin`   | `https://github.com/liliki2187/angos.git` | 常规开发仓库；历史上部分提交曾被 Secret Scanning / Push Protection 拦截，若 `push origin main` 失败请先读终端报错并按 GitHub 指引处理冲突或走 PR |
| `daydreamer` | `https://github.com/daydreamerguan/angus.git` | 用户侧快照/备份；若与本地 `main` 历史分叉，可把文档类变更推到 `daydreamer` 上新分支（例：`design-decisions-*`），避免误强推 |

**分支建议**：以本机 `git branch -a` 为准。常见情况是在 **`main`** 上日常提交；历史上曾使用过 `clean-main-20260325`，若远端已删除该分支，请以当前可在 `origin` 推送的分支为准。

**合并回 `origin/main`**：若有长期并行分支，优先 **GitHub Pull Request**，避免在非交互环境强推共享分支。

---

## 二、主入口与实验入口（路径已核对仓库布局）

原型与全链条 Demo 的根目录：**`design/prototypes/html/full-chain-demo/`**

| 用途 | 路径 / 参数 |
|------|-------------|
| 全链条可玩页（合并产物） | `design/prototypes/html/full-chain-demo/world-mysteries-full-chain.html` |
| 源码（改完需再合并） | 同目录下 `world-mysteries-full-chain-head.htm` + `world-mysteries-full-chain.js` |
| 合并产物 | 将上述 `head` + `js` 写入单一 `world-mysteries-full-chain.html`（可用本机脚本或编辑器；历史 dev-log 中有「合并后再打开」惯例） |
| **报纸填充实验**（跳过探索/合成） | **`design/prototypes/html/full-chain-demo/world-mysteries-full-chain.html?paperlab=1`**（或哈希 `#paperlab`） |
| **旧派遣配置回退**（仅供对照） | **`design/prototypes/html/full-chain-demo/world-mysteries-full-chain.html?dispatchold=1`**（默认路径已使用新版派遣配置） |
| 派遣配置候选排序实验页（待审阅，未合入全链） | `design/prototypes/html/labs/dispatch-configuration-p0p2-lab.html?v=20260521-dispatch-sort-only` |
| 世界探索 P0/P1 实验页（已审阅，并已合入全链） | `design/prototypes/html/labs/global-map-selection-ui-lab.html?v=20260521-world-explore-p0p1` |
| 骰子动画实验页 | `design/prototypes/html/labs/dice-animation-lab.html` |
| 骰面卡片判定实验页（待审阅，未合入全链） | `design/prototypes/html/labs/dice-judgement-card-lab.html?v=20260522-dice-card-party-layout` |

**归档 / 不推荐作真源**：

- `_obsolete/design/prototypes/html/full-chain-demo/newspaper-fill-lab.html`：旧独立实验页，仅供参考；请勿与现行 `design/prototypes/html/` 并行当真源。
- 仓库地图真源：`docs/onboarding/repository-map.md`（写明「原型在 `design/prototypes/html/`、根目录已无双击入口」）。

**分享给同事**：可打包该目录或使用已生成的离线 zip（若有）；解压后双击入口 html，**必须先完整解压**，勿在压缩包内直接双击。

---

## 三、近期已实现能力（摘要 · 可选读 dev-log）

- **探索**：按天「下一天」、派遣入队、到期任务队列判定；判定后回到全球地图。
- **区域**：地图点与侧栏事件联动；线索配队 + 骰子；派遣占用与可视化。
- **合成 / 编辑部**：合成台配方、报刊组版、结算；曾有 **topicKey / 情报消耗 / 同题疲劳** 等与 HTML 原型同步的迭代（详见近期 commit 与 `design/gdd/` 若有登记）。
- **原型载体**：默认在 **HTML 全链条**上验证玩法；Godot 运行时另有独立目录 `gd_project/`。
- **2026-04-28 首区任务类型验证**：北美禁区带曾试做 `白色调查 / 红色截稿 / 深度调查连续任务 / 高危五档 / 再追一次 bonus` 的 HTML 原型展示；截图说明页见 `docs/screenshots/2026-04-28-task-type-landing/index.html`。注意：其中“高危五档 / bonus”属于过渡原型，2026-05-06 设计主干已改为“高危任务（无黑骰、机制简单）”与“黑骰任务（深度链终局/特殊事件、四档结果）”分离；2026-05-08 又进一步收敛为：高危不再作为独立任务类型，而是非黑骰任务上的“危险标识 / 高难标记”，非黑骰任务统一最多 3 人。
- **2026-04-29~04-30 方向确认**：后续通用骰子与角色成长方向倾向改为 **角色专精 + 骰面数值**；角色数量少而精，剧情更多靠特殊事件、现象、深度任务推进。讨论全文见 `docs/讨论全文_20260427-0430_任务类型黑骰与角色专精.md`。
- **2026-05-09 深度链入口交互确认**：深度连续任务在地图上不再画完整路径或保留灰色旧环；地图只显示当前最新任务入口。入口采用规整叠层任务页，hover 只轻微预展开，点击后同一组卡片展开为“当前环 -> 下一条 ?”的线性链。已落地到 HTML 全链条原型，正式规则同步见 `design/gdd/exploration-and-node-dispatch.md` 与 `docs/设计采纳记录.md` A5。
- **2026-05-18 深度链地图点击语义修正**：用户指出地图深度链展开后，再点当前环应直接进入任务界面，而不是把链收起；收起可以通过点击任务之外的地图空白完成。完整全链原型已改为“第一次点开 / 展开后点当前环进入 / 地图空白收起”；后续又补充为：点击未解锁的 `?` 后续环应弹出 `该任务未解锁` 提示，不能沉默或收起。再次修正后，提示已从页面右上角全局 toast 改为锚定在被点击的未解锁环附近。相关原则已同步到 UI 指引、GDD、AI 协作错题本与设计采纳记录 A5。
- **2026-05-19 深度链旧环退出列表修正**：用户指出区域任务列表几乎全变成紫色，和“连续任务也就 3 个”的预期冲突。原因是已完成深度链旧环仍留在右侧当前任务列表，同时新解锁环也进入列表，历史阶段和当前入口叠加。已修正为：已完成的深度链旧环不再进入区域当前任务列表，列表只保留每条链最新可做的一环；地图层此前已隐藏已完成深度链旧环。规则已同步 `docs/onboarding/ui-interaction-guidelines.md`、`docs/onboarding/ai-collaboration-guidance.md`、`docs/设计采纳记录.md`。
- **2026-05-19 角色骰判定效果图语义纠偏**：当前最新的判定 UI 效果图方案中，`有效点` 被错误地和 `理性 / 洞察` 做成同一行同级进度条。用户已明确纠正：`有效点 / 达标目标 / 达标率` 是本次判定结果口径，属性条是贡献来源 / 需求拆解，不能因为控件形态相同而并列。下一版效果图必须改为父子结构：先显示本次是否过线的结果摘要，再用次级属性拆解解释有效点来源。规则已同步 `docs/onboarding/ui-interaction-guidelines.md`、`docs/onboarding/ai-collaboration-guidance.md` 与 `docs/设计采纳记录.md` A8。
- **2026-05-19 / 2026-05-21 / 2026-05-22 / 2026-05-25 角色骰实体动效改为实验页先行，并选择横轴急转落地**：曾尝试把“判定摘要 + 大骰盘 + 锁定 / 重投 + 实体摇骰”直接落到完整全链判定弹窗，但用户反馈表现奇怪，要求先不要落地到游戏。本体中的该轮骰子判定落地改动已撤回；当前改为独立视觉实验页 `design/prototypes/html/labs/dice-animation-lab.html`。用户随后指出单面薄片翻转和 CSS 3D 立方体仍然像卡牌 / 薄片；又指出 Three.js WebGL 实验页在实际打开方式下显示不出来，并明确要求“不改变原有判定界面布局，只是改成顶视角看骰子投入”。当前单文件 Canvas 顶视角方案虽然可直接双击打开并保留判定弹窗信息结构，但用户已明确否定其“立方体”表现：截图第一眼仍像几张面片，面模型没有建完。之后又指出修闭合体积时不能破坏旧版优点：旧版文字贴合立方体，新版文字错乱同样不合格；再指出各面透明、内部结构线干扰文字阅读也不合格；随后指出文字不能像临时屏幕标签，应当作为骰面贴图随面投影。2026-05-22 用户先偏好“斜轴翻滚”的动作方向但要求节奏像轴心急旋一样快速、短促、急停；后续审阅短促斜轴翻滚和无序急旋后，最新要求是把动效重置为清晰轴向对照：A 回到原来的方案一；B 保持方案一其它参数不变，只把纵轴旋转换成横轴旋转；C 做斜轴单向旋转，不能来回晃；D 新增“先纵轴再横轴”，前半段纵轴单向快转、后半段横轴单向快转。实验页已按该口径追加四案。2026-05-25 用户确认把方案二“横轴急转”落到完整全链判定弹窗：保持原判定布局与逐颗停骰 / 选择流程，只把摇骰阶段改为方案一速率、短摇动和急停节奏下的横轴翻转；不要混入斜轴、无序多轴或慢停。注意：本轮曾错误地把落地理解为“旧 2D DOM 骰子 + CSS 横轴变形”，用户明确否定；随后又错误地把实体骰模型放成大画布主舞台，用户再次明确只允许替换骰子模型与动效，不允许改变整个界面。已补规则要求实验页方案落地必须保持视觉主体和实现类别等价，同时保持原承载界面布局；若需要调布局必须先问。后续必须同时按“闭合立方体 / 不透明可读骰面 / 面内容贴图化 / 光影投影一致 / 落地接触可信 / 方案一节奏短促急停 / 只比较轴向和换轴顺序、不混入额外晃动噪声 / 与实验页基准截图等价 / 原弹窗布局不重排”验收。当前实现应只在原角色卡片里的骰子位置渲染 3D 模型骰。本轮复盘已追加通用规则：未来视觉总结要写成“症状 -> 错误判断口径 -> 可观察门槛 -> 可迁移规则”，避免只记录单个骰子问题而无法举一反三。
- **2026-05-25 角色卡骰子俯视结果面**：用户进一步要求完整全链判定弹窗中的骰子停止后采用俯视读法，即最终判定结果那一面必须完全朝上。后续检查不能只确认“有 3D 模型”和“文字可读”，还要确认结果文字 / 数值是否位于顶面本身，而不是侧面标签或临时屏幕贴字。
- **2026-05-27 主干试玩平面骰回归原因**：完整全链 HTML 已改为外部加载 `world-mysteries-full-chain.js`，而当前外部 JS 与入口版本号回到了旧的 `20260513-dice-roll-v7` 平面骰路径，导致主干试玩仍显示 `.roll-die` 文本骰。修复时必须改外部 JS、当前 HTML/CSS 与 `index.html` 入口版本号，不能再按旧的内联脚本合成方式覆盖文件。
- **2026-05-27 派遣配置候选卡悬停闪动**：用户指出划过候选角色牌时界面频繁闪动。根因是右侧 `dispatchHoverPreview` 在 `mouseenter` 时从一句话扩展为四格详情、`mouseleave` 又立刻缩回，改变了候选卡上方布局高度，反过来改变鼠标命中区。后续悬停预览必须预留详情态高度、保留上一次预览，并对连续划过加短延迟，不能让 hover 触发布局跳动。
- **2026-05-27 UX 老哥交付格式硬规则**：用户要求后续 UX 老哥意见必须在最终回复中可折叠保留，不能只给父级执行结果。后续只要调用 `ux_laoge` 或在当前线程按 `ux-diagnosis` 执行同等诊断，最终回复应包含 `<details><summary>UX 老哥诊断原文</summary>…</details>`，折叠块外再写“我如何执行 / 已落地 / 未采纳或待确认”。除非用户明确要求极简摘要，不得省略 UX 老哥正文。
- **2026-05-27 派遣配置去实验页冗余**：用户要求 UX 视角继续检查派遣配置，指出顶部宏观属性、候选卡上方重复文字等信息噪音过重。当前方向是让该页更像正式游戏界面：配置页隐藏不服务当前组骰池决策的宏观属性条；候选角色卡只保留头像、姓名、简短适配等级和 dice net；右侧面板只做最终复核，例如是否达标、还差什么、能否开始。不要再把候选 hover 结果常驻成“候选预览 / 达标率变化 / 来源变化 / 相关面”四格调试面板；详细贡献进入详情页或非常轻量的临时提示。2026-05-27 进一步修订：任务需求与覆盖 / 缺口的主承载位是右侧“需求覆盖 / 判定目标”，任务头只保留任务身份、耗时和特殊后果，不再重复 `需求 人脉2 / 洞察2`。
- **2026-05-27 根入口与组版页边界澄清**：用户明确当前根 `index.html` 承担开发导航，不按正式玩家入口移除实验目录；报刊组版页中“重新生成故事库 / 数量 / 报刊填入演示 / 版面模式 / 拟物增强”这类已无用调试控件可以删除；行动日志后续可能成为现实剧情故事文字的一部分，不能当作开发噪音直接删。当前已落地：完整全链组版页只保留故事库、版面、结算面板以及“清空版面 / 结算本期”，根入口与行动日志保留。
- **2026-05-11 地图与风险提示修正**：区域地图增加标签碰撞避让机制，优先保留深度链、黑骰、高危入口，线索与普通任务标签自动让位；已复查默认 / hover / 展开态与宽 / 中 / 窄视口。任务配置页风险预览改为按需求覆盖度与缺口判断，低贡献角色提示“无相关面”但不让整体风险反向升高。
- **2026-05-13 区域任务卡同步**：完整全链试玩已同步任务探索 UI 当前版；北美区域任务卡使用固定耗时槽位、红色任务右侧大倒计时、线索“调查 -> 新任务”，并去掉内部任务类型 / 默认规则冗余说明。2026-05-18 已进一步把深度链从普通标签流中抽离：不再显示 `下一页：?`，改为任务卡右侧专属块 `深度调查 / 当前任务 N`，避免剧透总长度并强化特殊任务槽位；随后移除区域任务卡内重复的右下角小动作按钮，改为整卡点击进入，避免按钮挤压右侧特殊状态块。最新截图见 `docs/screenshots/2026-05-18-region-card-entry/05-region-card-entry-no-buttons-region.png`。
- **2026-05-18 已派遣任务队列下沉**：用户指出已派遣任务仅靠状态标识仍容易和可点击任务混淆。完整全链区域任务列表已改为“可派遣任务主列表 + 已派遣 · 执行中次级队列”：执行中任务统一下移到弱框体内，取消主卡 hover / 点击语法，优先显示执行队员头像、执行中人员和几天后结算。后续又明确避免 `等待回收` 文案，因为正常情况下不鼓励玩家回收已派遣队员。截图见 `docs/screenshots/2026-05-18-region-assigned-queue/01-region-assigned-queue.png`；对应原则已同步 `docs/onboarding/ui-interaction-guidelines.md` §4.5、`docs/onboarding/ai-collaboration-guidance.md` 与 `docs/设计采纳记录.md` A9。
- **2026-05-13 全球地图选择 UI**：全球地图已从“点区域直接进入 / 右侧平铺列表”改为“先选区域，再进入选中区域”。右侧只显示选中区域详情，地图点与右侧区域小卡 hover / click 联动；详情固定展示可见目标、限时机会、线索、深度链；锁定区域显示进度 / 缺口并禁用进入按钮，“下一天”降级为次级操作。截图见 `docs/screenshots/2026-05-13-full-chain-global-map-selection-ui/`。
- **2026-05-13 派遣配置默认落地**：完整全链试玩默认使用新版派遣配置 UI，使用真实任务、真实角色骰、真实道具与真实派遣队列；页面围绕“本次骰池”、需求覆盖、缺口风险、临时资源和候选角色贡献排序组织，并在派遣配置页隐藏浮动“下一天”。旧版配置页保留 `?dispatchold=1` 作为回退 / 对照入口。默认落地截图说明页见 `docs/screenshots/2026-05-13-dispatch-default-ui/index.html`；改动标注试样见 `docs/screenshots/2026-05-13-dispatch-review-poc/index.html`。
- **2026-05-13 派遣全链回归修正**：默认新版派遣配置已跑完整路径：完整全链 -> 全球地图 -> 北美 -> “突发：雷达异常光点” -> 选择角色 / 一次性道具 / 临时线人 -> 开始调查 -> 回区域 -> 下一天 -> 到期结算 -> 回区域。修正了派遣入队后一次性道具骰只被消耗、未进入到期掷骰池的问题；现在队列记录会保存道具骰，结算时恢复到掷骰池，撤销未结算派遣会返还道具。真实截图与断言汇总见 `docs/screenshots/2026-05-13-dispatch-regression/index.html`。
- **2026-05-13 截图口径修订**：用户明确表示暂时不用考虑竖屏手机端使用，后续不要额外制作竖屏手机 / 极窄长页截图，以节省时间和 token。常规 UI / 原型交付优先覆盖主要桌面或实际目标视口；只有用户点名、任务本身涉及移动端 / 窄屏适配，或小视口是问题复现场景时再补。
- **2026-05-13 派遣配置布局收敛**：按“信息层级不能用并列模块偷懒”的 UI 原则，已把派遣页任务头从四个并列摘要格改为紧凑 meta；探索周剩余时间不再作为任务属性展示；红色截稿截止改为特殊警戒条；配置页隐藏全局“结束探索周”；开发态“新版派遣配置 / 真实任务”等标签移除；临时资源收进“本次骰池”下的“追加骰”；候选角色卡高度压缩。桌面主视口截图与断言见 `docs/screenshots/2026-05-13-dispatch-layout-pass/index.html`。
- **2026-05-13 截稿任务头再收敛**：用户指出“红色截稿”标签与红色警戒条重复，且关键数值“剩余 3天”不够明显。已将派遣页红色截稿任务头改为右侧高权重倒计时块 `截稿关闭 / 剩余 3天`，移除重复的“红色截稿”标签和旧红条，后果说明降级为一行文字。复验截图见 `docs/screenshots/2026-05-13-dispatch-deadline-header/index.html`。本次错误原因已补入 `docs/onboarding/ai-collaboration-guidance.md`，执行规则已补入 `docs/onboarding/ui-interaction-guidelines.md`。
- **2026-05-13 纠偏沉淀流程固化**：用户明确要求以后避免“让 AI 修改的内容没有反思、没有总结加入错题本和对应规则文档”。已在 `docs/onboarding/ai-collaboration-guidance.md` 固化“修改 / 纠偏后的四步沉淀检查”，并在 `docs/onboarding/ui-interaction-guidelines.md` 的更新规则中要求 UI 返工后检查错题本、对应规则文档、设计采纳记录、GDD 与交接文档；新对话最终回复前要说明沉淀结果或无需更新的原因。
- **2026-05-13 区域已派遣状态重构**：区域地图上的已派遣任务不再使用微型“派”字角标，改为地图点右上角显示派遣角色小头像组；右侧任务卡的“已派遣”移到任务名称右侧，不再混在耗时 / 危险 / 对手骰标签行。已跑真实路径：北美 -> “突发：雷达异常光点” -> 选择 3 名角色 -> 开始调查 -> 回区域；截图与断言见 `docs/screenshots/2026-05-13-region-assigned-avatars/01-region-assigned-state.png`。
- **2026-05-13 角色骰达标率修正**：用户试玩反馈“风险低也没有一次达标”。已确认旧风险预览使用潜在骰面覆盖，真实结算却逐属性硬达标，导致低风险实际可能只有 0% 到 15%。当时先改为普通任务按“相关骰面有效点 >= 任务目标”达标，并让风险预览基于同一公式计算实际达标率；该目标公式已在 2026-05-14 继续修正，以下一条为准。截图见 `docs/screenshots/2026-05-13-dispatch-chance-fix/01-dispatch-actual-chance.png`。对应规则已同步 `design/gdd/event-check-resolution.md`、`design/gdd/exploration-and-node-dispatch.md`、`docs/设计采纳记录.md`、`docs/onboarding/ui-interaction-guidelines.md` 和 `docs/onboarding/ai-collaboration-guidance.md`。
- **2026-05-19 风险标签分辨率修正**：用户指出 51 区外围公路中 3 人队与伪人单人都显示“风险：中”，容易误以为多人多骰没有帮助。已确认真实达标率从伪人单人约 33% 提升到三人约 54%，问题是粗粒度风险标签吞掉差异。当前派遣页风险徽标显示为 `风险：中 · 54%` 这类等级加实际达标率，说明区显示 `较单人最佳 +21%`，让多人收益直接可见。规则已同步 `docs/onboarding/ui-interaction-guidelines.md`、`docs/onboarding/ai-collaboration-guidance.md` 和 `docs/设计采纳记录.md`。
- **2026-05-14 首周派遣难度修正**：用户进一步指出“只改显示、不改难度”是治标不治本。已确认 2026-05-13 公式仍把需求属性数量当作硬下限，使“突发：雷达异常光点”适配 3 人队只有约 15% 达标率。已将普通任务目标改为 `max(需求总值 >= 4 ? 2 : 1, ceil(需求总值 / 3))`，移除属性数量硬下限；随后继续按任务档位落地，以下一条为准。复验截图见 `docs/screenshots/2026-05-14-dispatch-balance-fix/01-dispatch-actual-chance.png`。本次错误已补入 `docs/onboarding/ai-collaboration-guidance.md`，UI 风险规则、GDD、设计采纳记录与归档总集均已同步。
- **2026-05-14 任务档位难度分层落地**：用户指出普通任务应是最低难度，不应所有任务都要求 3 人满编才能通过。已将“普通任务以 1 到 2 名适配角色可玩为基准，3 人用于稳妥 / 提效；红色、高危、黑骰的难点来自时限、后果、反噬或资源占用，而不是基础成功率压死”同步到 `design/gdd/event-check-resolution.md`、`design/gdd/exploration-and-node-dispatch.md`、`docs/设计采纳记录.md` 和 `docs/onboarding/ai-collaboration-guidance.md`，并据此调整 HTML 原型任务数值。关键概率：`51 区外围公路` 1/2/3 人约 50%/66.7%/72.2%；`突发：雷达异常光点` 1/2/3 人约 33.3%/50%/54.6%，3 人加夜视约 63.6%；`罗斯威尔档案残页` 1/2/3 人约 50%/66.7%/77.8%；`330 末班车 · 乘客口述` 1/2/3 人约 50%/66.7%/77.8%。复验截图：`docs/screenshots/2026-05-14-dispatch-balance-fix/01-dispatch-actual-chance.png`、`docs/screenshots/2026-05-14-dispatch-balance-fix/02-ordinary-two-person.png`。
- **2026-05-14 派遣角色骰视觉语法修正**：用户指出派遣候选角色卡中的骰面退回普通横向 chip 行，而不是已采纳的长尾十字架 / 骰子立方体摊开。已查明原因：代码已有 `diceNetHtml()` 与 `.dice-net`，角色详情使用了它，但新版派遣配置的候选角色卡与已选角色槽仍调用 `diceFacesHtml(..., "staff-pick-faces")` / `pool-dice-faces`。已将候选角色卡与已选角色槽改回 compact dice net，一次性道具仍保留横向 chip 摘要；Playwright 断言候选卡 dice net 数量为 8、横向 staff chip 为 0，已选槽 dice net 数量与已选人数一致。截图见 `docs/screenshots/2026-05-14-dispatch-dice-net/01-dispatch-dice-net-red.png`、`docs/screenshots/2026-05-14-dispatch-dice-net/02-ordinary-dice-net-two-person.png`。对应规则已同步 `docs/onboarding/ai-collaboration-guidance.md`、`docs/onboarding/ui-interaction-guidelines.md`、`docs/设计采纳记录.md`。
- **2026-05-18 派遣候选队员位置稳定修正**：用户指出派遣界面点击队员后，该成员会跑到更靠前的位置，导致难以记住谁是谁。已移除 `dispatchLabSortedStaff()` 中“已选角色优先”的排序条件，候选角色仍在进入页面时按任务贡献排序，但选中 / 取消只改变高亮和上方“本次骰池”槽位，不改变候选池位置；同分角色用固定角色基础顺序兜底。Playwright 验收记录点击中后段队员、继续选择、取消选择后的候选 id 顺序完全一致；截图见 `docs/screenshots/2026-05-18-dispatch-stable-candidates/01-dispatch-stable-candidates.png`。规则已同步 `docs/onboarding/ui-interaction-guidelines.md`、`docs/onboarding/ai-collaboration-guidance.md`、`docs/设计采纳记录.md`。
- **2026-05-18 派遣人数上限保护修正**：用户指出点击超过当前任务最高派遣人数后，系统自动取消一个已选角色来选上新角色，破坏玩家确认权。已移除新版派遣配置、旧版回退配置和角色详情弹窗里的自动 `shift()` 替换逻辑；达到人数上限后继续点新角色，只弹出提示“本任务最多派遣 N 人，请先手动撤下一名已选角色”，并保持当前已选队伍不变。悬停预览也不再模拟自动替换后的风险，而是提示人数已满。规则已同步 `docs/onboarding/ui-interaction-guidelines.md`、`docs/onboarding/ai-collaboration-guidance.md`、`docs/设计采纳记录.md`。
- **2026-05-18 临时线人入口迁移修正**：用户指出“追加骰”里一次性道具可以再次点击撤回，但右侧临时线人点选后直接进入阵容且不可撤回；随后进一步要求雇佣后右侧不再继续显示按钮。已拆分“本周已雇佣”和“本次是否加入骰池”：第一次点击会雇佣并加入本次骰池；雇佣后追加区的临时线人购买入口消失，临时线人进入候选角色列表；后续加入 / 撤回通过候选角色卡或已选槽完成。人数已满时尝试雇佣仍沿用人数上限提示，不自动替换。二次修正：临时线人入口消失后，追加区剩余道具按钮保持固定三槽列宽，不随意拉伸，空位用于表达已使用 / 移除的资源入口。规则已同步 `docs/onboarding/ui-interaction-guidelines.md`、`docs/onboarding/ai-collaboration-guidance.md`、`docs/设计采纳记录.md`。
- **2026-05-14 全球地图角标安全区修正**：用户指出全球地图锁定区域按钮的 `80%` / `82%` 进度角标遮挡区域名称。已将地图点角标从未预留空间的右上角绝对定位改为按钮内独立徽标槽位，并新增 Playwright 审计脚本检查 `.map-point-title` 与 `.event-dot` 的矩形不重叠且留出安全间距。截图见 `docs/screenshots/2026-05-14-global-map-badge-safe-zone/01-global-map-badge-safe-zone.png`。本次错误已补入 `docs/onboarding/ai-collaboration-guidance.md`，执行规则已补入 `docs/onboarding/ui-interaction-guidelines.md`，设计原则已同步 `docs/设计采纳记录.md`。
- **2026-05-14 全球地图地区选择器与日循环重构**：用户指出全球地图右侧复用区域任务卡，导致玩家自然想点任务；但该页真实功能只是选择地区并进入。已将右侧改为“选择取材地区”头部、地区选择卡、地区概况数值和最多 3 条只读情报预览；hover 地区只高亮，不再临时切换详情，点击才改变选中地区。随后用户继续指出主功能按钮必须大且通常放右下角，已将“进入选中地区”改为右下角大主按钮并放大箭头图标。最新修正：全球地图不再显示 `下一天`；时间推进只保留在当前地区任务上下文；推进一天或到期结算后默认回到当前地区。2026-05-18 进一步把地区页 `下一天` 从右侧任务列表框体内移出，改成整个区域面板底部的独立 `日程推进` 条，按钮文案为 `推进到第 N 天 / 结束探索周`，并显示已派遣、到期结算和突发倒计时变化摘要，避免被误读为任务选择确认按钮；随后区域页顶部的 `结束探索周 · 进入编辑部` 已移除，改为 `返回世界探索层`，点击返回世界取材地图，不触发周末或编辑部。2026-05-19 进一步统一层级返回按钮尺寸：`返回回合简报`、`返回世界探索层`、`返回区域` 都使用更清晰的导航返回样式，保持按钮自身放大而不重排页面布局。Playwright 断言全球详情内 `region-task-card` 为 0、可操作预览节点为 0、`btnNextDayGlobal` 为 0，并检查进入地区后 `btnNextDayRegion` 存在。截图见 `docs/screenshots/2026-05-14-region-day-flow/01-global-no-next-day.png`、`docs/screenshots/2026-05-14-region-day-flow/02-region-next-day-context.png`、`docs/screenshots/2026-05-18-region-day-advance/01-region-day-advance-strip.png`。对应规则已同步 `design/gdd/exploration-and-node-dispatch.md`、`docs/onboarding/ai-collaboration-guidance.md`、`docs/onboarding/ui-interaction-guidelines.md`、`docs/设计采纳记录.md`。
- **2026-05-21 世界探索 P0/P1 方案合入全链**：用户审阅 `global-map-selection-ui-lab.html?v=20260521-world-explore-p0p1` 后确认“挺好的，落地”。已将阶段导航、地图主语、右侧选中地区详情、唯一主按钮 `进入探索`、成本 / 收益 / 条件 / 挑战 / 信息 / 状态语义标签、本周行动日志折叠合入 `world-mysteries-full-chain.js` 和可打开的 `world-mysteries-full-chain.html`。后续修正：`对手骰` 不再归入条件，显示为 `挑战：对手骰N`；条件标签统一 `条件：探索2` 这类冒号口径。完整全链页面的全球地图不恢复 `下一天`，右侧情报仍是只读预览，具体任务选择只在进入地区后发生。
- **2026-05-21 派遣配置候选排序实验页**：用户提供派遣配置 UX 诊断后，先要求按 P0 / P1 / P2 做实验页；随后审阅指出只有候选区 `任务贡献 / 达标提升 / 未派遣优先` 排序标签不错，其它实验性布局不要。当前已把 `design/prototypes/html/labs/dispatch-configuration-p0p2-lab.html?v=20260521-dispatch-sort-only` 收回为“现有派遣页壳层 + 候选排序标签”实验，未合入完整全链。
- **2026-05-22 骰面卡片判定实验页**：用户确认先按骰子判定 UX 诊断中的最小路径做实验页，验证骰面卡片选择、固定计入槽、需求实时联动和提交前结果预览。随后用户纠正“3 到 5 人页面布局”讨论对象是骰子判定而非任务派遣；当前 `design/prototypes/html/labs/dice-judgement-card-lab.html?v=20260522-dice-card-party-layout` 已追加 3/5 名参判角色切换：最多 5 个固定参判槽，3 人判定显示未参判占位，5 人判定打开全部槽位，切回低人数不自动取消已计入骰面。未合入完整全链；一键推荐、黑骰扩展、投骰动效与结果演出仍需后续单独确认。
- **2026-05-18 全球地图父子层级落地**：用户指出全球地图右侧上方“地区选择”和中部“地区信息”仍像两个同级强模块，地区信息应从属于地区选择且不应有明显可点击表现。已抽象为“父级选择器与从属详情不能同级化”规则，并同步 `docs/onboarding/ui-interaction-guidelines.md`、`docs/onboarding/ai-collaboration-guidance.md`、`docs/设计采纳记录.md`。HTML 原型已把 `#globalRegionDetail` 移入 `.global-side-head`，并在二次修订中进一步把地区选择强化成带箭头、hover、选中态和连接三角的按钮组；详情改为“当前选中地区”的附属信息：透明背景、左侧归属线、轻量属性行和普通情报列表，去掉预览行右侧的动作式文字与弱版卡片背景。Playwright 验收确认详情在选择器下方、详情 `cursor: default`、选择卡 `cursor: pointer`、选中按钮有箭头 / 连接指示、属性区为 flex 行、情报行背景透明、预览区无按钮 / 链接 / 交互类、全球地图无 `下一天`、右下角进入按钮仍明显且可进入地区。截图见 `docs/screenshots/2026-05-18-global-selector-detail/01-global-selector-detail.png`。
- **2026-05-18 全球地图可进入 / 未解锁状态反馈落地**：用户指出全球地图中已解锁可点击地区和未解锁地区差别不大，包括地图点和右侧地区选择按钮，没有凸显“已解锁可点击”和“未解锁不可点击”的区别。已抽象为“可用与不可用状态必须拉开行动距离”规则，并同步 `docs/onboarding/ui-interaction-guidelines.md`、`docs/onboarding/ai-collaboration-guidance.md`、`docs/设计采纳记录.md`。当前已落到完整全链主界面：可进入地区使用实体按钮 / 地图点、金色主色、箭头、hover 和选中连接指示；未解锁地区改为锁定 / 幽影语法，使用虚线边框、低饱和低透明、锁标识、缺口文案，并且不再作为 `button` 参与选择。Playwright 验收确认锁定地区选择卡 2 个且锁定按钮 0 个、锁定地图点 2 个且锁定地图按钮 0 个，点击锁定东亚地区后选中详情仍保持“北美禁区带”。截图见 `docs/screenshots/2026-05-18-global-availability-main/01-global-availability-main.png`。
- **2026-05-14 核心体验与玩法设计原则真源新增**：围绕“发现未知 -> 接近未知 -> 反噬显形，由周刊组织串联”的核心体验，新增 `design/gdd/core-experience.md` 作为 AI 优先的核心体验 One Page 与跨模块最高层锚点；新增 `design/gdd/gameplay-design-principles.md` 作为任务、骰子、黑骰、深度链、路线与反噬的玩法规则设计准绳。后续接玩法 / UI / 内容 / 美术 / 系统设计前，优先阅读这两份文档。

---

## 四、更新日志与设计采纳

- **开发日志**：`docs/dev-logs/YYYY-MM-DD.md`，收工尽量补一篇并推送。
- **用户采纳的设计点子**（长期索引）：[`docs/设计采纳记录.md`](./设计采纳记录.md) — 对话里明确采纳/待定/进阶的条目由 AI **当次追加**，勿只留在聊天里。

---

## 五、索引文档（详读从这些走）

| 文档 | 用途 |
|------|------|
| [`docs/onboarding/repository-map.md`](./onboarding/repository-map.md) | 仓库分层、原型路径 |
| [`docs/onboarding/README.md`](./onboarding/README.md) | 入门阅读顺序 |
| [`docs/onboarding/git-collaboration.md`](./onboarding/git-collaboration.md) | Git 协作 |
| [`docs/onboarding/ai-collaboration-guidance.md`](./onboarding/ai-collaboration-guidance.md) | AI 与用户偏好硬规则 |
| [`docs/onboarding/功能改动截图指引.md`](./onboarding/功能改动截图指引.md) | 游戏/原型改动后的真实截图与说明页生成流程 |
| [`docs/设计采纳记录.md`](./设计采纳记录.md) | 设计点子采纳索引 |
| [`docs/讨论全文_20260427-0430_任务类型黑骰与角色专精.md`](./讨论全文_20260427-0430_任务类型黑骰与角色专精.md) | 本轮长讨论全文（任务类型、黑骰、专精骰方向） |
| [`docs/screenshots/2026-04-28-task-type-landing/index.html`](./screenshots/2026-04-28-task-type-landing/index.html) | 任务类型落地截图与文字说明 |
| [`design/gdd/`](../../design/gdd/) | 玩法正式真源（若与原型冲突以协商为准） |
| [`design/gdd/core-experience.md`](../../design/gdd/core-experience.md) | 核心体验 One Page，后续 AI 与模块设计的最高层体验锚点 |
| [`design/gdd/gameplay-design-principles.md`](../../design/gdd/gameplay-design-principles.md) | 玩法规则设计原则，指导任务、骰子、黑骰、路线与反噬设计 |

---

## 六、一键开场（复制到新对话）

```
请接手 d:\angos（《世界未解之谜周刊》/ Angus 合并仓库）。

请先读：docs/会话交接_当前状态.md、design/gdd/core-experience.md、design/gdd/gameplay-design-principles.md；必要时再读 docs/onboarding/repository-map.md 与 docs/onboarding/ai-collaboration-guidance.md。

Git：优先在已与 origin 同步的开发分支上工作；若 push 被拒，先看 GitHub Secret Scanning / Push Protection 提示或改用 PR。

HTML 主入口所在目录：design/prototypes/html/full-chain-demo/
- 全链条：world-mysteries-full-chain.html（合并产物）；改逻辑请先改同目录下的 .htm + .js 再合并。
- 报纸填充实验：world-mysteries-full-chain.html?paperlab=1

设计采纳沉淀：docs/设计采纳记录.md
近期长讨论全文：docs/讨论全文_20260427-0430_任务类型黑骰与角色专精.md
截图说明页：docs/screenshots/2026-04-28-task-type-landing/index.html

请在我接下来描述的具体任务上继续，不要擅自大范围重构。
```

---

**全文完。**
