# 发刊编辑界面正式黑白结构稿 v2

> 产物类型：`formal_ui_structure_wireframe`
>
> 本稿只呈现玩家正式界面中真实存在的字段、组件和状态，不包含开发注释、结构说明、像素尺寸或英文变量名。它冻结功能结构，但仍不是最终视觉包装或 Godot 生产界面。

## 相对 v1 的正式化修正

- 删除左栏底部“当前来源 / 剩余篇数 / 拖回 / 排序”等结构说明；这些规则只通过真实状态反馈表达。
- 左栏完整显示 8 张 `288×92` 候选卡；8 篇时不出现滚动条，10 / 12 篇时才启用真实滚动。
- 删除顶部和底部的结构稿标签，以及中央 / 右栏所有英文变量名和像素尺寸。
- 右栏只保留玩家需要的 `本期头版 / 发行预览 / 发行风险 / 送印检查 / 签批送印`。
- 普通候选卡不重复显示“候选”；状态槽只在 `已选稿 / 不可用` 等差异状态出现。

## 合同边界

现行 `candidate_card v1.2.0` 只冻结六个屏幕位置。本稿提出 `v1.3.0` 升版候选：新增第 7 / 8 张卡的屏幕 y 坐标 `800 / 904`；卡片 `288×92`、内部槽位、命中区和状态语义均保持不变。用户确认本结构前，不修改生产合同真源。

## 当前状态

- 默认候选：8 篇全部可见，无滚动条。
- 版面：3/6 已上版，三个空位。
- 来源：A04 为唯一选稿，不放大、不重排。
- 目标：内页 2 为唯一合法目标，只使用双边线。
- 复核：发行结果待重算，签批送印禁用。

运行 `render_wireframe.py` 可重新生成 1920×1080 结构板与审计文件。
