# 发刊编辑界面整屏包装 Mock v1

> **状态（2026-07-17）：`rejected_for_functional_capacity / packaging_reference_only`。** 用户指出本稿把大量候选报道压成单张接近正方形的大卡，违背已冻结的 `candidate_card 288×92` 与多候选滚动容量；同时本稿使用了双 CTA 预览链，不符合当前单一 `primary_cta` 合同。本页只保留为包装探索与偏差证据，不得作为当前结构真源、Godot 落地依据或后续包装母版。当前功能结构候选见 `../weekly-editorial-functional-black-structure-v1/`。
>
> 产物类型：`filled_state_text_mock`。
>
> 本页只用于验证桌面 `1920×1080` 下的整屏包装、信息层级、图文融合与状态辨识；不是 Godot 生产界面、切图源或最终美术真源。

## 本稿验证什么

- 冻结现有 `320 / 1040 / 360` 三列、双版常显、六版位与原子换稿合同。
- 把外围程序化面板包装为“候选稿夹—打开的周刊—签批夹”三种职业物件。
- 使用 `5/6` 选择态同时展示稿件来源、合法目标与待重算后果。
- 区分“当前已知硬阻断为 0”和“公开取向 / 同题疲劳尚未计算”。
- 让“计算发行预览”成为当前唯一可用主动作；“签批送印”在预览完成前禁用。

## 明确边界

- 不改 Godot 代码、运行时交互或 manifest。
- 不新增报道图；复用现有 `1001–1006` 报道资产。
- CSS 夹具、纸边与硬影只用于本轮视觉提案；正式 Godot 落地仍需无字组件资产和状态 atlas。
- 用户确认本稿前，不进入正式运行时包装。

入口：`index.html`
