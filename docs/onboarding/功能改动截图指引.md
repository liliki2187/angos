# 功能改动截图指引

> **用途**：后续 AI 在完成游戏 / 原型可见改动后，用统一流程生成真实截图与文字说明，避免每次重新摸索，也避免用户逐张翻找。
> **适用范围**：HTML 原型、Godot 原型、可运行 demo、可视化交互改动。

## 1. 交付硬规则

每次游戏 / 原型有可见改动后，最终交付必须包含：

1. **真实运行截图**：从实际页面、游戏窗口或运行时抓取，不能只贴设计稿或口头描述。
2. **文字说明**：说明每张截图“看什么、改了什么、玩家应感知到什么”。
3. **展示方式**：1-3 张截图可直接在对话里展示并配说明；多于 3 张截图时，必须生成一个 `index.html` 或等价说明页，把截图按玩家流程串起来。
4. **验证说明**：最终回复里说明截图位置、验证方式、是否有未覆盖的风险。
5. **复杂 UI 标注**：复杂页面、分阶段 UI 重构或“改动点不容易肉眼定位”的交付，应补一张程序化标注截图；标注基于真实运行页面的 DOM / UI 元素坐标绘制，不使用 AI 生图作为验收依据。

交付时必须确认截图在对话里以图片形式展示，而不是只提供文件路径。Windows 路径用于说明时可以保留，但 Markdown 图片标签应使用应用可渲染的绝对路径写法，例如 `![说明](D:/angos/docs/screenshots/.../01.png)`，不要使用反斜杠路径作为图片地址。

推荐目录：

```text
docs/screenshots/YYYY-MM-DD-简短主题/
  01-xxx.png
  02-xxx.png
  index.html
```

## 2. 截图前检查

截图前先确认：

- 当前任务的验证载体是什么：HTML、Godot，还是其他 demo。
- 用户最关心的改动点在哪些界面首次出现。
- 是否需要覆盖“点击前”和“点击后”的差异。若玩家必须点击才能理解机制，必须截点击后的状态。
- 默认不额外制作竖屏手机 / 极窄移动端截图；当前阶段暂不考虑竖屏手机端使用。只有用户明确要求、任务本身涉及移动端 / 窄屏适配，或改动点明显依赖小视口布局时，才补对应截图。
- 是否有新手引导、弹窗、随机事件会遮挡关键画面；若遮挡，应在截图脚本里关闭或稳定处理。

## 3. HTML 原型截图流程

优先用浏览器真实渲染截图。若项目没有 Playwright / Puppeteer，可用 Chrome / Edge 的 DevTools Protocol 临时脚本，不必新增依赖。

### 3.1 启动浏览器

检查浏览器路径：

```powershell
Test-Path "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe"
Test-Path "C:\Program Files\Google\Chrome\Application\chrome.exe"
```

可用参数：

```text
--headless=new
--remote-debugging-port=<port>
--user-data-dir=<临时目录>
--allow-file-access-from-files
--window-size=1440,1100
```

### 3.2 通过 CDP 控制页面

Node 24 已有 `WebSocket`，可直接连接：

1. 启动浏览器。
2. 访问 `http://127.0.0.1:<port>/json/list` 获取 `webSocketDebuggerUrl`。
3. 调用：
   - `Page.enable`
   - `Runtime.enable`
   - `Emulation.setDeviceMetricsOverride`
   - `Page.navigate`
   - `Runtime.evaluate`
   - `Page.captureScreenshot`

截图脚本应放在临时文件里，完成后删除，不要把一次性脚本留在仓库根目录。

### 3.3 稳定截图建议

- 截图前写入 localStorage 关闭新手引导：

```js
localStorage.setItem("wm_angos_no_tutorials_v1", "1");
localStorage.setItem("wm_angos_tutorial_intro_offer_done_v1", "1");
```

- 每次点击后等待目标状态，而不是固定 sleep：

```js
await waitFor("!document.getElementById('view-setup').classList.contains('hidden')");
```

- 对随机结算截图，必要时临时固定 `Math.random` 序列，让结果稳定复现。
- 截图前 `window.scrollTo(0, 0)`，除非要展示长页中段。

### 3.4 程序化改动标注截图

复杂 UI 改动建议生成三类截图：

1. **旧版同状态**：用于确认改动前玩家看到什么。
2. **新版同状态**：用于确认真实页面改成了什么。
3. **新版标注版**：用脚本在真实页面上叠加编号、框线和短说明。

标注实现建议：

- 用 Playwright / CDP 获取真实页面渲染后的元素位置，例如 `getBoundingClientRect()`。
- 在页面上临时注入 overlay、label、banner，再截图；截图后删除临时脚本，不把一次性标注逻辑留在正式代码里。
- 标注文字要短，只说明“改了哪里 / 玩家要看什么”，不要写成长说明书。
- 标注图只作为交付说明；真实验收仍以无标注截图、运行路径和自动/人工检查为准。
- 如果一张标注图会遮挡太多内容，拆成多张局部标注图或改用截图汇总页。

## 4. HTML 原型常见坑

### 4.1 外部 JS 与 HTML 内联脚本不同步

本项目部分 HTML 原型历史上存在“HTML 内联脚本”和同名外部 JS 并存的情况。截图前必须确认当前页面实际执行的是改过的脚本。

检查方法：

- 搜索 HTML 是否存在大段 `<script> ... </script>` 内联逻辑。
- 若改动落在外部 `world-mysteries-full-chain.js`，但 HTML 仍执行旧内联脚本，应先改成加载外部脚本，或同步改内联脚本。
- 截第一张图后要肉眼确认新标签 / 新文案确实出现；没有出现就不要继续批量截图。
- 若页面通过 `<script src="...js?v=...">` 或 CSS 版本参数加载外部资源，改动外部 JS / CSS 后必须同步更新版本参数，避免用户浏览器继续使用缓存旧资源。截图脚本可用额外 query 参数绕过缓存，但这不能替代正式入口自身的版本号更新。
- 自动断言通过不等于截图交付通过。最终回复前必须打开或查看即将发送的截图，肉眼确认关键文案、位置、状态和改动点正确；尤其是刚修过的争议文案，不能只相信脚本返回的 `audit` 文本。
- 返工截图不要覆盖旧文件名。即使内容已重新生成，也应使用新的截图文件名，例如从 `01-...png` 递增到 `02-...png`，避免对话端或浏览器缓存继续显示旧图。

### 4.2 视觉目标反向检查

截图验收不能只确认“页面有东西、脚本有动画、像素非空”。对任何视觉改动，都必须确认截图里的对象、状态和层级是否真的像用户要求的东西。

通用检查方法：

- **需求拆词**：先提取用户需求里的视觉名词、动作词和禁止项，例如“立方体”“实体摇骰”“可点击更明显”“不要像等待回收”“不要像同级模块”。
- **条件列表**：把每个词翻译成可观察条件。物体看形状、体积、材质、光影、遮挡和运动；UI 控件看位置、大小、形状、对比度、hover、cursor、文案和状态；信息层级看父子关系、边框重量、缩进、背景和留白。
- **旧版优点回归**：返工前先列出旧版、参考图或用户认可截图里已经成立的视觉优点，并在新截图中逐项确认没有退化。不要只检查本轮新修的问题；例如骰子不能只检查闭合体积，还要检查文字、骰点或贴图是否仍贴合面透视。
- **禁止误读**：写出至少一个反向读法，并在截图里主动寻找它是否仍存在。例如“不能像纸片”“不能像可点击任务”“不能像主操作”“不能像鼓励玩家回收”。
- **局部裁切**：除整页截图外，裁切关键对象区域，用 100% 与放大视图检查；如果用户截图指出的是局部问题，就按同一局部重新检查。
- **遮挡与噪音检查**：局部裁切时必须检查是否有内部结构线、背面线、装饰纹理、阴影或调试辅助图形穿过核心信息。骰子示例：若透明面或线框边穿过文字 / 骰点，即使立方体闭合，也算未通过。判断“不透明”不能看实现意图；要看截图中背景网格、轨迹线、隐藏边是否仍能透过对象面内区域被读出来。
- **材质归属检查**：如果对象内容声称在某个面上，必须检查它是否像该面材质的一部分，而不是投影后叠上的屏幕标签。骰子示例：文字和骰点应随面一起缩放、旋转、裁切、遮挡；若文字相对面角度漂移、对位不准或偶发消失，说明实现层级不对。
- **多态检查**：涉及交互时，至少检查默认、hover / 展开、选中、禁用、错误或提示状态中与改动相关的状态。不要只截最理想的一帧。
- **实现不等于感知**：不要用“代码里有三维计算”“有投影”“有动画”“DOM 断言通过”替代玩家视觉判断。若截图第一眼仍会被读成禁止误读项，验收失败。
- **旧错回归**：对本对话里被用户明确纠正过的问题，后续同类截图必须先查旧错误是否复发；不能只检查新改动。
- **局部语义回归**：如果对象由多个视觉子项共同成立，必须逐项检查子项是否一致。骰子要检查外轮廓、面内容、阴影、接触关系和动作；按钮要检查形状、文案、位置、hover 和禁用态；地图点要检查点、标签、角标、连线和提示位置。
- **复盘抽象**：若同一视觉对象被连续纠偏，截图交付后必须总结“本轮反复错在哪里”。总结要从具体 bug 提炼到通用检查项：例如骰子 3D 化不能只写“文字要贴好”，而要写“内容归属必须随对象材质一起变形；不透明必须看截图是否透出背景；修复一个维度必须回归旧版优点”。未来 AI 应能把这些规则迁移到其他 UI / 模型 / 动效。

### 4.3 弹层遮挡

奖励弹窗、日推进弹窗、确认弹窗、新手引导弹窗都可能阻塞截图流程。截图脚本应提供：

- `closeRewards()`
- `closeTutorials()`
- `clickConfirmOk()`
- `waitForVisible(selector)`

### 4.4 文件路径

Windows 下生成页面链接时，截图汇总页内优先使用相对路径：

```html
<img src="01-region-overview.png" alt="..." />
```

不要写绝对盘符路径，避免换机器后失效。

## 5. 截图汇总页结构

`index.html` 至少包含：

- 标题：本次改动主题与日期。
- 摘要：这次改了哪些可见功能。
- 导航：按玩家流程跳转。
- 每张截图：
  - 编号与标题；
  - 对应文件；
  - 改动点；
  - 玩家应感知到什么；
  - 相关机制标签。

推荐一张图一个区块：

```html
<article>
  <h3>02 · 白色调查配置</h3>
  <p>说明玩家在配置页能看到白色调查的行为差异。</p>
  <img src="02-white-investigation-setup.png" alt="白色调查配置页截图" />
</article>
```

## 6. Godot 原型截图流程

若改动落在 Godot：

1. 优先运行现有项目启动命令，不擅自切换验证载体。
2. 若可用自动化截图命令，使用项目已有脚本；没有脚本时，用 Godot 运行窗口截图。
3. 截图必须覆盖：
   - 首次可见入口；
   - 玩家点击后的说明 / 操作状态；
   - 结算或反馈状态。
4. 同样生成 `docs/screenshots/YYYY-MM-DD-主题/index.html`。

## 7. 最终回复模板

最终回复简短说明：

```text
已生成截图说明页：
`docs/screenshots/YYYY-MM-DD-主题/index.html`

覆盖：
- 入口/点击前状态
- 点击后的配置或操作说明
- 结算/反馈状态

验证：
- ReadLints 无报错
- 运行 / 语法检查通过
```

若无法截图，必须说明原因，并给出替代验证方式；不能只说“已改完”。

## 8. 对话负载与新对话提醒

截图越多，对话越容易变慢。执行截图交付时应主动控制对话负载：

- 1-3 张关键截图可直接贴在对话里。
- 超过 3 张时，优先生成截图汇总页，并在最终回复中只贴关键图或给出汇总页路径。
- 为节省时间与对话负载，常规 UI / 原型回归只覆盖玩家主要使用视口；不要默认追加竖屏手机端或极窄长页截图。
- 如果当前对话已经积累了大量截图、长链路 UI 讨论、反复截图重跑或明显开始未响应，应直接提醒用户“建议新建对话继续”，并附上最小交接信息：当前目标、已改文件、截图目录、验证命令和下一步。
- 不要为了维持同一对话而继续塞大量图片；可读性和响应稳定性优先。

## 9. 完成前检查清单

- [ ] 截图来自真实运行界面。
- [ ] 每个可见改动至少有一张对应截图。
- [ ] 最终发送前已肉眼查看关键截图，确认截图内可见文案 / 状态就是本次最终版本。
- [ ] 对视觉对象 / 动效 / 模型 / UI 状态，已做“用户需求拆词 -> 可观察条件 -> 禁止误读 -> 局部裁切”的检查。
- [ ] 若经历返工或用户强反馈，已把问题总结为“症状 -> 错误判断口径 -> 可观察门槛 -> 可迁移规则”，并更新对应文档。
- [ ] 1-3 张截图已用可渲染的 Markdown 图片语法直接嵌入对话；不是只写文件路径。
- [ ] 多张截图已整理成汇总页。
- [ ] 复杂 UI / 分阶段改动已提供程序化标注截图，或说明为什么不需要。
- [ ] 截图数量已控制；若当前对话过重，已建议用户新建对话。
- [ ] 汇总页内图片链接使用相对路径。
- [ ] 最终回复给出汇总页路径，并对关键截图做直接展示；不要只列图片文件名。
- [ ] 临时截图脚本、临时浏览器 profile 已清理。
- [ ] 已运行必要的 linter / 语法检查。
