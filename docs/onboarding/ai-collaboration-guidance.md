# AI 协作指引：如何更精准理解当前用户

> **用途**：给后续接手本仓库的 AI / 新对话提供一份可执行的协作约束与偏差复盘。  
> **对象**：不是给用户看的总结稿，而是给 AI 自己看的工作指引。  
> **维护方式**：每当用户给出新的负反馈、纠偏指令或暴露出新的误判模式，都应补充到本文对应章节。  
> **最后更新**：2026-05-12

---

## 1. 文档定位

这份文档记录的不是“项目功能需求”，而是**当前用户的沟通方式、判断标准、常见雷区，以及 AI 在本项目中最容易犯的偏差**。

目标只有两个：

1. 让后续 AI 在接手时更快抓住用户真正要的东西。  
2. 把已经暴露过的误判模式写成硬规则，避免重复犯错。

如果本文与临时想法冲突，**优先遵守本文**，除非用户在新对话里明确推翻。

涉及 UI、交互、原型呈现、视觉实验页时，还必须同时读取并执行 [`ui-interaction-guidelines.md`](./ui-interaction-guidelines.md)。本文保留“为什么犯错”的复盘，UI 文档提供“具体怎么做”的执行规则。

---

## 2. 用户的核心偏好画像

### 2.1 用户最看重“玩家是否能明显感知到变化”

用户判断“做出来了没有”，不是看规则是否已经在后台生效，而是看：

- 玩家是否在界面上看得到；
- 玩家是否在操作中感知得到；
- 玩家是否能明确分辨“这次和之前不一样”。

因此：

- **规则存在** 不等于 **玩法成立**；
- **结算文案提到** 不等于 **机制被感知到**；
- **内部状态变化** 不等于 **玩家体验发生变化**。

### 2.2 用户偏好“最小改动验证”，不喜欢顺手改大范围外观或载体

当用户说“完善一下某个玩法”时，默认理解为：

- 优先在**当前熟悉的载体**上验证；
- 优先做**最小行为改动**；
- 不要顺手把 UI、节奏、入口、运行方式一起重做。

尤其是：

- 如果用户原本在看 HTML 原型，**不要擅自切到 Godot 运行时做演示**；
- 如果用户要的是“补骰子玩法”，**不要顺手重做阶段切页、整体布局或产品壳层**。

### 2.3 用户希望“首个可玩区域就能体验到核心机制”

用户非常重视“上手即得”。

如果某套新机制需要：

- 解锁后续区域；
- 玩很久才能遇到；
- 或藏在不容易触发的节点里，

用户通常会认为“实际上没做出来”。

因此：

- 优先把新机制集中到**第一个可玩区域**；
- 优先把 showcase 节点放到**最早可触达的位置**；
- 不要把主要机制分散在后续地图里等玩家慢慢打。

### 2.4 用户要“可思考、可控制”的操作权

用户对“自动替玩家推进”的系统反应会比较强，尤其是：

- 自动重投；
- 自动跳过思考阶段；
- 很短的时间窗口；
- 玩家还没理解就已经进入下一段。

因此：

- 玩家需要决策时，必须有**明确的停顿**；
- 最好有**明确按钮**而不是自动继续；
- 锁定 / 重投 / 确认这类动作，优先给玩家手控权。

### 2.5 用户对界面偏差很敏感

如果用户要求的是玩法补强，而结果把界面视觉体验改得面目全非，用户会认为：

- 做偏了；
- 没有抓住重点；
- 甚至怀疑“你是不是在做另一件事”。

因此：

- 改玩法时默认**不改大 UI 风格**；
- 若必须改展示层，优先局部嵌入，不重做整体布局；
- 如果要换载体或大改交互壳层，必须先明确征得用户同意。

---

## 3. 本次对话里用户提出过的关键问题与指令

以下不是完整逐字转录，而是对后续最重要的需求轨迹摘要。

### 3.1 关于骰子玩法本身

用户反复要求：

- 先发散，再收束；
- 把可操作维度和机制按层级梳理；
- 新机制不能只停留在“系统算了”，必须让玩家明显体会到；
- 没有直接对抗时，也要做出“像被对抗”的感觉；
- 关卡类型（考古 / 科学调查 / 异常接触等）应有不同机制偏好。

### 3.2 关于当前验证载体

用户一开始接受把玩法落进当前 demo，但在实际看到效果后明确表达：

- 不希望因为做骰子玩法，导致界面观感和熟悉的网页版本差太远；
- 重点是**保留熟悉体验的前提下补玩法**；
- 如果 Godot 包体 UI 太开发态，应优先回到 HTML 原型继续做展示。

### 3.3 关于“玩家能感受到”这件事

用户提出的关键判断标准包括：

- 干预、污染等不能只存在于最后结算文字里；
- 每个小机制应该有明显触发时机；
- 机制之间要有间隔，不要一闪而过；
- 单颗骰子要足够清晰，骰子太多时会让所有机制失效；
- 应有手动锁定某个骰子的操作；
- 锁定后要有明确的“开始重投”按钮，不能自动替玩家继续。

### 3.4 关于内容分布

用户明确要求：

- 把主要新机制优先放在北美禁区；
- 因为那是第一个能玩到的区域；
- 不要把 showcase 主要放在后续区域。

### 3.5 关于后续协作

用户希望形成一份长期文档，记录：

- 本次对话里所有关键问题和指令；
- AI 为什么没做好、没考虑到；
- 用户思考方式和偏好；
- 后续新反馈继续往里追加；
- 并且要让别的对话或 AI 接手时也能受到约束。

---

## 4. 本次对话里 AI 已暴露的主要偏差

### 偏差 1：把“补玩法”误做成“换载体验证”

#### 表现

用户原本更熟悉 HTML 原型，但 AI 为了“让用户能玩到”，优先把新玩法塞进了 Godot `weekly_run`。

#### 为什么这是问题

这改变了用户熟悉的体验壳层。即使规则本身没错，用户看到的也会先是：

- 界面不一样；
- 节奏不一样；
- 操作感觉不一样；

从而掩盖掉用户本来真正想看的“骰子玩法变化”。

#### 后续硬规则

- 如果用户最初讨论的是 HTML 原型或网页体验，默认先在 **HTML** 验证。  
- 不得以“更容易运行”为理由，擅自把玩法迁到另一种载体演示。  
- 如果必须换载体，必须先明确征得用户同意。

### 偏差 2：把“规则被算进去了”误当成“玩家已经体会到了”

#### 表现

AI 先把：

- 类型包、
- 副轴、
- 干预、
- 余波、
- 链式节点

都写进了逻辑层，但玩家看到时仍然觉得“还是原来的简单作废骰”。

#### 为什么这是问题

因为这些变化主要在：

- 计算层；
- 结果文案；
- 状态写回；

而不是在玩家视野里的阶段演出中。

#### 后续硬规则

- 任何新机制如果只在结算文字或状态里可见，默认视为**未做完**。  
- 规则层完成后，必须立刻追问：  
  - 玩家在哪个时刻第一次能感知到它？  
  - 玩家会误以为它还是老机制吗？  
  - 没看说明能不能区分出来？  

### 偏差 3：低估了“骰子数量过多”对可读性的破坏

#### 表现

早期实现仍然使用大量骰子，导致：

- 单颗骰子存在感过低；
- 被干预、被污染、被保住的具体骰子看不清；
- 锁定决策没有重量。

#### 为什么这是问题

当骰子过多时：

- 任何单颗机制都会被淹没；
- 反馈再多也只是噪声；
- 玩家不会觉得自己在“做选择”，只会觉得系统在“播结果”。

#### 后续硬规则

- 只要玩法想强调单颗骰子、锁定、干预或污染，优先先压缩总骰数。  
- 在没有强理由前，把单次判定总骰数控制在 **10 个以内**。  
- 若强调“角色骰”，优先按角色出骰，而不是按属性点数直接堆骰。

### 偏差 4：过早自动推进，剥夺了用户思考窗口

#### 表现

锁定阶段一开始只有 2 秒自动重投。

#### 为什么这是问题

用户明确重视：

- 可思考；
- 可慢慢看；
- 可手动确认；

自动推进会直接破坏这类体验。

#### 后续硬规则

- 玩家需要决策时，默认不要自动继续。  
- 锁定 / 重投 / 确认类交互，优先提供明确按钮。  
- 如果有时间限制，必须先得到用户明确认可。

### 偏差 5：没有足够早地把“核心机制集中到首个区域”当作硬要求

#### 表现

早期把一些 showcase 放到了东亚或更后面区域。

#### 为什么这是问题

用户想要的是：

- 马上打开就能试；
- 第一张图就能覆盖主要机制；

否则就会被判断为“实际上玩不到”。

#### 后续硬规则

- 新机制的第一轮验证，默认优先集中到**首个可玩区域**。  
- 若需要 showcase 节点，也优先放在首区。  
- 后续区域可以留第二版或补充版机制，但不能承载第一轮验证的主要内容。

### 偏差 6：补玩法时顺手改了太多展示层

#### 表现

在 Godot 阶段，AI 一度同时改了：

- 规则层；
- 任务面板文案；
- 阶段显隐；
- 禁用态样式；
- 整体流程切页表现。

#### 为什么这是问题

这会让用户无法分辨：

- 是玩法变了；
- 还是整个界面被重做了。

#### 后续硬规则

- 如果用户要求是“补玩法”，默认展示层改动只做**局部嵌入式增强**。  
- 不主动做全局 UI 重组、阶段切页或视觉风格迁移。  
- 一旦发现实现开始往“另一个任务”扩张，必须停下来收束。

---

## 5. 当前用户的思考方式与决策风格

### 5.1 用户习惯先发散，再收束

当用户谈新系统时，通常会先：

- 大量发散机制方向；
- 看可能性空间；
- 再要求压成分层、路线、范围。

因此 AI 在前半段可以发散，但一旦用户开始要求：

- 总结、
- 梳理、
- 正式文档、
- 组合建议，

就必须从脑暴切到收束模式。

### 5.2 用户偏好“先做出可玩的最小体验，再慢慢加层”

用户不反对复杂机制，但很明确地希望：

- 先有一个可玩的第一层；
- 再看是否需要更复杂的层；
- 不要一上来把所有层都做进同一套演示里。

### 5.3 用户非常看重“感知到”

用户并不满足于：

- 文档写明了；
- 代码里算到了；
- 结果页写出来了。

用户真正要的是：

- 玩的时候能看出来；
- 能操作到；
- 能记住发生了什么；
- 能感觉到“这次和以前不同”。

### 5.4 用户会持续纠偏，并希望 AI 能把纠偏转成永久规则

用户不是只想要一次性修正，而是希望 AI：

- 把反馈抽象成规律；
- 写进文档；
- 让以后别的 AI 也遵守；
- 避免每次都重新教一遍。

因此，收到用户强反馈后，AI 应主动做：

1. 纠正当前实现；  
2. 把问题上升为规则；  
3. 更新长期协作文档。

---

## 6. 后续协作硬规则（必须执行）

以下规则是从本次对话里提炼出来的**长期约束**。

### 6.1 关于载体选择

- 默认优先在用户当前讨论和熟悉的载体上验证。  
- 不擅自从 HTML 切到 Godot，或从 Godot 切到 HTML。  
- 如果要换载体，先明确说明原因并征得用户同意。

### 6.2 关于展示与感知

- 新机制必须明确回答：  
  - 玩家在哪个时刻第一次感知到它？  
  - 玩家在哪个界面理解到它？  
  - 它是否只存在于结算文案？  
- 若答案是“只在结算文案里”，则视为未完成。

### 6.3 关于范围控制

- “补玩法”不等于“顺手重做 UI”。  
- 默认只做与用户明确目标直接相关的最小改动。  
- 一旦实现开始扩张到：
  - 全局 UI、
  - 阶段切页、
  - 入口重构、
  - 运行时迁移，
  就必须停下来重新确认。

### 6.4 关于首区试玩

- 第一轮机制验证优先集中在**首个可玩区域**。  
- showcase 节点优先也放在首区。  
- 如果用户还没玩到，就不要把关键体验放到后面区域。

### 6.5 关于玩家决策权

- 只要玩家有“锁定 / 重投 / 确认”决策，默认给明确按钮。  
- 不自动 2 秒倒计时，不替玩家自动推进。  
- 如果用户没明确要求快节奏，就优先让玩家“慢慢看、慢慢选”。

### 6.6 关于骰子数量

- 需要强调单颗骰子的玩法时，先压缩总骰数。  
- 默认目标：单次判定总骰数不超过 10。  
- 角色基础骰优先于属性点直接堆骰。

### 6.7 关于反馈循环

- 用户一旦指出“体会不到”“还是原来的”“太快了”“界面变了”，必须优先判断这是：
  - 感知层问题，
  - 节奏问题，
  - 载体问题，
  - 还是范围失控问题。  
- 不要立刻再加新规则，先处理感知与节奏。

---

## 7. 后续追加规则

从现在开始，每次用户给出新的强反馈，都应按以下模板补一条：

### 新增反馈模板

- **时间**：YYYY-MM-DD  
- **用户反馈**：用户原话的简要概括  
- **暴露问题类型**：载体 / 感知 / 范围 / 节奏 / 可读性 / 其他  
- **AI 当时偏差**：一句话说明  
- **新增硬规则**：以后必须怎么做  
- **影响范围**：HTML / Godot / 文档 / 所有会话

### 2026-04-28：从采纳记录转方案前必须先复述用户评注意图

- **用户反馈**：用户指出「没有按照我对每条的评注想法来推进具体方案」，要求先理解评注和想法，再进行方案撰写。
- **暴露问题类型**：文档 / 范围 / 其他。
- **AI 当时偏差**：把 `docs/设计采纳记录.md` 中的条目机械扩写成字段与实现清单，忽略了用户评注里的适用范围、层级区分、体验意图和不泛用约束。
- **新增硬规则**：当用户要求把采纳条目转成方案时，AI 必须先按条目复述「用户已采纳的核心意图 / 限定范围 / 不该做什么 / 玩家应感知到什么」，确认理解后再写落地方案；不得直接跳到规格文档或代码字段。
- **影响范围**：文档 / 所有会话。

### 2026-05-09：地图类界面不能用抽象圆斑替代空间语义

- **用户反馈**：用户指出区域任务图“显示效果太差”，希望背景替换为世界地图背景。
- **暴露问题类型**：感知 / UI / 可读性。
- **AI 当时偏差**：为了快速表达任务点分布，用抽象彩色圆斑充当地图背景，导致玩家无法把任务点和世界空间建立联系。
- **新增硬规则**：地图、区域、节点总览等承载空间关系的界面，必须优先使用可识别的地图语义（世界地图、区域轮廓、航线/经纬网、地形或城市结构），不要只用抽象光斑、圆区或纯装饰背景代替。若只是临时占位，也要明确让玩家看出“这是地图”。
- **影响范围**：HTML / Godot / UI / 所有会话。

### 2026-05-09：深度连续任务地图入口应表达“当前入口 + 潜在链”，不要一次摊开整条链

- **用户反馈**：用户指出深度连续任务在地图上的表现很差，不应在地图界面完整展示所有链路节点；地图应只显示当前最新任务，用叠层卡片等方式暗示“多个任务合一”，点击后再展开任务链，而且后续任务应随完成逐步显现。
- **暴露问题类型**：感知 / UI / 认知门槛 / 节奏。
- **AI 当时偏差**：把“深度链”理解成需要在地图上画出完整路径，于是提前暴露多个节点、阶段编号和连线，造成画面杂乱、剧透后续阶段，也让玩家误以为这些都是当前可操作任务。
- **新增硬规则**：地图层只承担入口识别和当前行动选择；连续任务应显示“当前可做的一环”，并用叠层、折角、堆叠阴影、轻微 hover 错位等视觉语言暗示背后有链。点击入口后再展示任务链详情：已完成环可见，当前环可操作，下一环最多以“？”预告，未到达的后续环不得提前完整展示。完成一环后，地图标识应替换为下一环，而不是保留旧环或同时显示整条链。
- **影响范围**：HTML / Godot / UI / 任务链设计 / 所有会话。

### 2026-05-09：深度链叠层交互应从规整堆叠“形变”为线性链，不要另开详情面板

- **用户反馈**：用户指出斜向、扑克牌式的叠层不规整；希望叠层在交互时流畅排列出线性的链，而不是另外出现一个界面展示任务链。
- **暴露问题类型**：感知 / UI / 动效 / 操作连续性。
- **AI 当时偏差**：把“点击后展开任务链”做成了按钮下方的额外抽屉，导致入口对象和链路对象割裂；叠层本身也使用了过于随意的斜向纸牌语法，不像规整任务页。
- **新增硬规则**：连续任务入口的交互应优先采用同一对象的空间形变：默认态是规整对齐的任务页叠层；hover 只做轻量预展开，让几张卡略微错开以提示“可展开”；点击后才完整排列成线性任务链。不要把链路信息放到完全独立的下方面板里，除非用户明确要求详情面板。动效要让玩家感知“这一叠任务正在展开为链”，而不是“又弹出了另一个 UI”。
- **影响范围**：HTML / Godot / UI / 动效 / 所有会话。

### 2026-04-28：游戏改动交付必须附真实截图与文字说明

- **用户反馈**：用户指出每次游戏改动后再让他翻找图片对应很麻烦，要求后续改动后附上改动部分的截图和文字说明，并形成截图指导文档。
- **暴露问题类型**：感知 / 文档 / 交付方式。
- **AI 当时偏差**：只生成零散截图文件，未主动整理成可快速浏览、可对照改动点的说明页。
- **新增硬规则**：后续每次游戏/原型有可见改动，交付时必须提供真实截图与文字说明；1-3 张截图可直接在对话里展示并配说明，超过 3 张截图必须整理成一个可打开的 `index.html` 或同等汇总页，说明每张截图对应的改动点、玩家应看到什么、涉及的机制。截图操作优先参考 `docs/onboarding/功能改动截图指引.md`。
- **影响范围**：HTML / Godot / 文档 / 所有会话。

### 2026-04-29：判定机制先做玩法创意，不在小技能里塞叙事

- **用户反馈**：用户指出黑骰设计“判定应该是简单的成功失败，过程中不涉及继续在判定中叙事”，重点应放在大叙事中的技能机制表达；例如鬼关卡可以让黑骰偷走己方骰子，时间悖论关卡可以使用时机破坏型黑骰。
- **暴露问题类型**：玩法设计 / 范围 / 可读性。
- **AI 当时偏差**：把黑骰面拆成大量带叙事解释的小效果（证词翻供、官方注视、录音花屏等），导致机制碎、结算中叙事过多，玩法创意反而被文案和小修正淹没。
- **新增硬规则**：设计判定机制时，先保证核心判定仍简单清晰（成功 / 失败或少量大档位），机制创意应是大颗粒、可操作、可被玩家一眼理解的规则变化；叙事应在关卡主题、任务链、结果选择和后续事件中表达，不要把每个骰面都写成一段小叙事。
- **影响范围**：玩法设计 / HTML / Godot / 文档 / 所有会话。

### 2026-04-29：高级报刊 UI 不能只做干净留白，页面本体要有插画与装饰信息量

- **用户反馈**：用户指出“整体界面风格已经差不多了，但是现在太素太简单了”，希望杂志报刊页面加入插画、装饰等，并参考带角色插画、道具大图、复古电台、章印、边栏和杂志排版的界面。
- **暴露问题类型**：视觉 / 感知 / 可读性。
- **AI 当时偏差**：把报刊组版界面推向了现代高级平面和大留白，但中央报纸页面缺少足够的叙事插画、装饰层和内容密度，导致“正式界面”看起来像干净模板而不是可玩游戏页面。
- **新增硬规则**：后续制作报刊、杂志、编辑部、档案类 UI 时，若用户没有明确要求极简，应在保持结构清晰的前提下给页面本体加入可辨识的插画、道具图、章印、胶片小图、边栏标签、标题装饰、印刷纹理和叙事性视觉锚点；高级感不等于空白，必须让玩家一眼感到“这是有内容的报刊页面”。
- **影响范围**：HTML / Godot / 图像生成 / 所有会话。

### 2026-04-29：独游视觉需要更强风格化与记忆点，不只做成熟参考板

- **用户反馈**：用户指出当前风格稿“整体不够风格化”，独游需要特殊的风格化，应更可爱、更有趣、更有创意、更吸睛，同时保持世界未解之谜的特点。
- **暴露问题类型**：视觉 / 感知 / 美术方向。
- **AI 当时偏差**：把视觉方向收敛成了较稳妥的高级档案板和复古报刊素材集合，虽然贴题但缺少一眼记住的独特图形语言、趣味角色化符号和可传播的风格钩子。
- **新增硬规则**：后续做 Angus 视觉方向时，不能只满足“高级、贴题、可用”，必须主动寻找独游级记忆点：例如角色化的未解之谜符号、荒诞可爱的危险物件、夸张比例的 UI 控件、可复用的图形母题、强色彩策略和带黑色幽默的微文案；风格化应服务“危险小报包装现实”的主题，而不是退回普通恐怖档案或中性报刊设计。
- **影响范围**：HTML / Godot / 图像生成 / 文档 / 所有会话。

### 2026-04-29：怪异元素要图形化讨喜，避免 Q 版写实带来的不适感

- **用户反馈**：用户指出上一版风格稿“有些趣味了，但是不讨喜”，例如眼球外星人虽然 Q 版但仍偏写实，观感不适；希望参考图中更讨喜、更图形化、更商业化的角色与包装语言。
- **暴露问题类型**：视觉 / 感知 / 美术方向。
- **AI 当时偏差**：把“怪诞可爱”理解为把恐怖/异形元素缩小和 Q 版化，但保留了写实眼球、肉感结构或怪物质感，导致可爱与不适互相抵消。
- **新增硬规则**：后续处理未解之谜、外星、怪影、眼睛等符号时，应优先使用扁平图形、粗轮廓、低细节、商品贴纸化和包装图标化表达；避免湿润眼球、写实皮肤、真实触手、牙肉、血管、密集孔洞等生理质感。可爱怪异应像品牌吉祥物、彩票图案、信息图标或零食包装符号，而不是缩小版恐怖生物。
- **影响范围**：HTML / Godot / 图像生成 / 文档 / 所有会话。

### 2026-04-29：卡通风格应靠表情包、周边和纸媒活动页语法提升讨喜度

- **用户反馈**：用户指出当前“卡通风格还是要找找，现在还没有那么讨喜”，并提供《重返未来：1999》表情包、联动活动页、商品图册等参考。
- **暴露问题类型**：视觉 / 感知 / 美术方向。
- **AI 当时偏差**：虽然降低了恐怖感并加入贴纸化表达，但角色仍偏泛化吉祥物，没有抓住参考图里“表情包式夸张表情、毛绒/周边化怪物、纸媒活动页包装、复古装饰边框与现代角色贴纸混搭”的讨喜来源。
- **新增硬规则**：后续探索 Angus 的可爱怪诞风格时，应优先参考“表情包包体 + 复古纸媒活动页 + 周边商品图册”的组合：角色使用大头短身、粗黑描边、半闭眼/无奈脸/夸张张嘴、软毛团或玩偶质感、简单手套手脚；版式使用旧纸、复古花边、撕纸、波点、爆炸贴、商品格和活动横幅。怪异感通过荒诞身份与黑色幽默文案表达，不通过写实怪物细节表达。
- **影响范围**：HTML / Godot / 图像生成 / 文档 / 所有会话。

### 2026-05-07：玩法落地必须单独评审呈现层与操作感

- **用户反馈**：用户指出目前最大问题是，实际落地后基础功能一般大致符合预期，但呈现效果、UI 交互、呈现内容和方式存在很大问题，影响玩家对新内容的感知，甚至出现操作没有明显变化的问题。
- **暴露问题类型**：感知 / UI 交互 / 呈现方式 / 验收流程。
- **AI 当时偏差**：容易把“规则和基础功能实现”视为落地完成，没有把玩家第一次看见、第一次操作、第一次理解该机制的体验单独作为验收对象。
- **新增硬规则**：后续任何玩法或原型落地，都必须在实现前后单独检查“呈现层验收”：入口是否显眼、操作是否与旧玩法有可感知差异、关键反馈是否发生在玩家决策现场、界面文案是否说明当前为什么不同、截图是否覆盖玩家第一次感知到新内容的时刻。若只是后台规则变化或结算后文字变化，默认视为未完成。
- **影响范围**：HTML / Godot / 文档 / 所有会话。

### 2026-05-11：地图标签必须有避让机制，不能只靠手工坐标

- **用户反馈**：用户指出区域地图中线索标签与深度链入口发生堆叠，要求采用机制避免标签堆叠。
- **暴露问题类型**：感知 / UI / 可读性 / 地图布局。
- **AI 当时偏差**：把任务点坐标当作静态摆放问题处理，忽略了任务入口变宽、线索标题变长、动态任务新增后会产生碰撞。
- **新增硬规则**：地图层只要同时存在多种任务点、线索点或可展开入口，就必须在渲染后检查标签碰撞；优先保留深度链、黑骰、高危等高优先级入口的位置，普通任务与线索标签让位。不要把“调一个固定坐标”当成长期方案。验收必须覆盖默认态、hover 态、点击展开态，以及至少宽 / 中 / 窄三种视口；只用一张桌面截图验收等同于未验完。
- **影响范围**：HTML / Godot / UI / 地图任务呈现 / 所有会话。

### 2026-05-11：任务配置页风险提示不能因加入低贡献角色而反向升高

- **用户反馈**：用户指出同一任务选择 2 人时风险为中，加入第 3 人后反而变成高，直觉上不合理。
- **暴露问题类型**：规则口径 / 感知 / 派遣决策反馈。
- **AI 当时偏差**：用“相关骰面 / 总骰面”的比例直接标风险，导致低匹配角色会拉低比例，让玩家误以为多派人会让任务本身更危险。
- **新增硬规则**：配置页整体风险应优先看任务需求覆盖度与缺口，而不是总骰面占比。低贡献角色应作为“无相关面 / 贡献低 / 占用人数槽”的提示呈现；除非规则明确规定会引发额外代价，否则加入角色不应让整体风险提示变差。
- **影响范围**：HTML / Godot / UI / 派遣配置页 / 角色骰提示 / 所有会话。

### 2026-05-12：关键信息固定位置不等于做成高视觉重量控件

- **用户反馈**：用户指出任务卡左侧“耗 / 2天”上下分行后像按钮且显得臃肿，效果反而不如原来。
- **暴露问题类型**：UI / 可读性 / 信息层级 / 视觉重量。
- **AI 当时偏差**：为了保证“耗时固定在左侧中位”，把耗时做成了过大的两行徽章，导致它抢占类型图标和任务标题的注意力，也容易被误认为可点击控件。
- **新增硬规则**：固定关键信息位置时，必须同时控制视觉重量，并优先沿用当前界面里已经成立的布局。若问题只是“2天语义不清”，应先把文案改成“耗时 2天 / 剩余 2天”，不要立刻重排整张卡片。只有倒计时、严重危险、当前可操作状态这类强时效信息才允许使用更高权重组件。
- **影响范围**：HTML / Godot / UI / 任务卡 / 所有会话。

### 2026-05-12：符号化信息必须有读法，不能重复表达同一语义

- **用户反馈**：用户指出任务卡左侧底部数字不知道是什么意思，两个感叹号显得冗余，按钮之间没有对齐。
- **暴露问题类型**：UI / 可读性 / 信息架构 / 对齐。
- **AI 当时偏差**：把对手骰数字、危险图标等压缩到左侧信息轨的小圆点里，但没有文字或稳定图标语境，导致玩家无法读出含义；同时正文又保留“危险”标签，形成重复强调。
- **新增硬规则**：任务卡符号化应遵守“一语义一位置”。危险、黑骰、对手骰等需要解释的压力信息应放在有文字读法的标签区；不要把孤立数字或重复感叹号作为独立信息。操作按钮应使用固定列或固定宽度对齐，且不能因对齐导致文字换行。
- **影响范围**：HTML / Godot / UI / 任务卡 / 所有会话。

### 2026-05-12：不要把局部语义问题扩成整体布局重构

- **用户反馈**：用户指出老图里“截止在右边且比较大的 UI 设计很好”，原本唯一问题只是耗费天数只写“2天”不明确，质疑 AI 为什么直接大改。
- **暴露问题类型**：范围 / UI / 感知 / 需求理解。
- **AI 当时偏差**：把“找更明确的剩余时间表达”误扩展成“重新组织任务卡信息架构”，丢掉了用户认可的右侧大倒计时块和原有卡片层级。
- **新增硬规则**：当用户指出某个 UI 元素“语义不明确”时，第一轮修正应只改该元素的表达方式、标签文本、图标或 tooltip；除非用户明确要求重排，否则不要改变已被认可的空间结构。改前要先识别用户真正想保留的优点。
- **影响范围**：HTML / Godot / UI / 所有会话。

### 2026-05-12：玩家界面只显示会改变决策的信息，不展示默认规则说明

- **用户反馈**：用户指出任务卡仍有大量冗余信息，例如“可能发现新任务”和调查转新任务示意重复，“白色调查 / 可反复追踪 / 失败保留”属于设计层内容，不必给玩家看；玩家只需要知道哪些任务是例外，例如突发任务会消失；“高压”等组件含义不明。
- **暴露问题类型**：UI / 信息架构 / 冗余信息 / 玩家视角。
- **AI 当时偏差**：把内部任务类型、默认规则和例外规则都平铺到卡片上，误以为“说明更全”就是“更清楚”，导致玩家需要过滤大量不影响当前选择的信息。
- **新增硬规则**：任务卡面向玩家时，只显示会改变当前决策的信息：耗时、需求、对手骰、可用操作、倒计时、危险/黑骰等明确例外。默认规则不显示，例如“普通任务失败保留、可反复追踪”不写；同一含义只保留一处表达，例如倒计时已经写“剩余 2天”时，不再写“过期消失”。含义不明确或没有直接玩法后果的标签（如“高压”）必须删除或改成明确规则词。
- **影响范围**：HTML / Godot / UI / 任务卡 / 所有会话。

### 2026-05-12：实验页也必须继承正式原型已验证的可读性保护

- **用户反馈**：用户指出视觉实验页里仍然出现地图标签堆叠，质疑之前要求处理的堆叠机制为什么没有生效。
- **暴露问题类型**：UI / 实验页 / 验收遗漏 / 可读性。
- **AI 当时偏差**：把“正式全链原型已修复标签避让”误当成全项目已解决，却新建了一个静态实验页，仍用手写绝对坐标，没有同步接入避让机制；第一次补救又被 CSS transform 过渡影响，候选位置测量一直读到动画起点。
- **新增硬规则**：只要实验页复用地图、任务卡、可展开入口等正式 UI 语义，就必须继承对应的基础保护机制：标签避让、碰撞审计、状态截图、响应式检查。不能因为页面是临时实验而退回手工坐标。做避让时若元素带 transform 过渡，候选位置应使用“基础矩形 + 偏移量”计算，或临时禁用过渡，不能直接读动画中的 `getBoundingClientRect()`。
- **影响范围**：HTML / UI / 实验页 / 地图任务呈现 / 所有会话。

### 2026-05-12：同一类功能标识必须固定在同一信息槽

- **用户反馈**：用户指出“耗时”有的任务显示在右边，有的显示在左边，同类功能性标识的位置必须统一。
- **暴露问题类型**：UI / 信息架构 / 可读性 / 一致性。
- **AI 当时偏差**：为了迁就不同任务卡的局部布局，把同一类信息放在不同槽位：普通/深度任务耗时在标题右侧，线索/突发/黑骰耗时在正文标签区，导致玩家需要逐卡寻找信息。
- **新增硬规则**：任务卡中的同类功能标识必须固定槽位，不得随任务类型漂移。耗时统一放在标题下方第一枚标签；需求标签跟在耗时后；对手骰/危险/黑骰等压力信息放在同一标签行；右侧保留给状态、倒计时或主要操作，不与耗时混用。若某任务没有该信息，槽位可以缺省，但已有信息不能换位置。
- **影响范围**：HTML / Godot / UI / 任务卡 / 所有会话。

### 2026-05-12：删减信息后必须回收空间并重测间距

- **用户反馈**：用户指出删除冗余信息后，红色任务卡仍保留过大的空隙，导致这一项比其他任务松散。
- **暴露问题类型**：UI / 布局 / 可读性 / 后续整理。
- **AI 当时偏差**：只删除了文字和标签，没有同步调整原先为这些内容预留的网格行高、占位和间距；右侧倒计时仍参与正文布局，撑大了卡片内部空白。
- **新增硬规则**：任何 UI 信息删减后，必须同步检查并回收布局空间：行高、gap、padding、min-height、grid/slot 占位、绝对/流式布局关系都要复核。可视组件若只是状态角标或倒计时角标，应避免参与正文流导致空行。交付前截图必须确认“内容少了，空间也变紧凑”，不能只确认文字消失。
- **影响范围**：HTML / Godot / UI / 任务卡 / 所有会话。

---

## 7A. 设计采纳沉淀机制（长期固定机制）

> 自 2026-04-27 起，本机制纳入长期协作约束，所有后续 AI / 新对话都必须遵守。

### 7A.1 沉淀文档

- **文件位置**：[`docs/设计采纳记录.md`](../设计采纳记录.md)
- **文档定位**：用户在讨论中**已采纳 / 待定 / 进阶 / 合并 / 撤回** 的设计意见的长期索引；不是讨论原文，也不是规格文档，而是**决策留痕**。

### 7A.2 何时写入（触发条件）

只要用户在讨论中出现以下任一**明确表态**，AI 在当次回复结束前必须把对应条目追加进该文档：

- 「这个采纳」「加入 …」「作为 …」「可以考虑作为 …」「合并到 …」；
- 「先留着」「待定」「回头再想」「加入待定区」；
- 「作为后期进阶」「作为某类型设计」「作为某角色特性」；
- 「撤回」「不要这条了」「改成 …」（撤回/修订路径，见 7A.5）。

**反例（不写入）**：

- AI 单方面的设想或推理（即使写得再合理）；
- 仅用户在发散讨论里"提到"但未表态的内容；
- 仅 AI 在回复里"建议"而用户未确认的内容。

### 7A.3 分类与编号

文档内分五节，编号即未来引用 ID：

| 节 | 编号前缀 | 含义 |
|----|----------|------|
| 一、已采纳 · 通用机制 | `A` | 影响通用玩法的核心采纳 |
| 二、已采纳 · 特殊任务/特殊角色类型 | `B` | 限定在某类任务或某类角色上的采纳 |
| 三、后期进阶选项 | `C` | 方向上认同，但不在第一阶段实现 |
| 四、待定区 | `D` | 暂存，回头再考虑 |
| 五、机制库（备查，未采纳） | `M` | 仅备查的外部机制参考 |

新条目优先放在已有同类节的**末尾**，编号沿用前缀 + 自增数字（如 `A3`、`B4`）。

### 7A.4 条目格式

每条至少包含以下字段：

- **来源**：会话日期 / 上下文，例如 `2026-04-27 骰子机制讨论`；
- **机制**：一段话说明该机制本身；
- **应用范围**：通用 / 特定任务类型 / 特定角色 / 进阶 / 待定；
- **状态**：已采纳 / 待定 / 进阶 / 合并 / 已修订 / 已撤回；
- 如有合并、撤回、修订，再追加 **合并备注** / **修订备注**。

具体写法示例参见现有条目（如 A1、B1、C2）。

### 7A.5 修订与撤回

- 用户调整或撤回某条意见时，**保留原条目**，在条目末尾追加：
  - `已修订（YYYY-MM-DD）：…新表述…`
  - 或 `已撤回（YYYY-MM-DD）：…原因…`
- **不允许直接覆盖**或删除原条目，确保决策路径可追溯。
- 引用关系（如 B2 与 B1 合并）也应保留并标记。

### 7A.6 与其他文档的关系

- 本机制与 7 节「新增反馈模板」**互不替代**：7 节记的是"AI 偏差与用户偏好"，本机制记的是"具体玩法/机制的采纳决策"。两者并存。
- `docs/设计采纳记录.md` **不是规格文档**，仅做索引；某条目要落地为代码或字段时，应另起规格文档（或直接落到 `design/gdd/`），并在采纳条目里**回引规格文档**。
- 如果用户后续要求把某条采纳"转为正式真源"，AI 应把该条同步到对应 `design/gdd/` 文档，并在原条目里标注 `已升级为真源：路径`。

### 7A.7 编外内容先确认

- 用户明确纠偏（2026-05-07）：明显属于编外内容、外部商业化素材、活动外观命名、临时文案方案，或与 Angus 当前项目风格 / 玩法真源关系不明确的内容，**不要默认写入** `docs/设计采纳记录.md`。
- 这类内容即使用户选定了名称或文案，也应先判断是否属于项目长期设计决策；若不确定，先询问用户“是否需要沉淀到项目记录”，得到明确同意后再追加。
- 若已经开始准备写入但用户说明“这是编外内容”，应立即停止写入；已写入的需按用户要求修订或移除。

### 7A.8 待细化工作流点子：美术风格稿与视觉组件词典

- 用户明确留档（2026-05-07）：先作为点子记录，后续再详细制定正式美术风格稿和“视觉组件词典”的规则。
- 初步方向：先确立一份可复用的美术风格稿；后续每次玩法改动先在玩法 UI 原型中落地，暂不做完整 GUI 视觉包装；再基于新的玩法原型截图和美术风格稿生成目标效果图；最后把效果图拆解为可实现的视觉组件、状态、符号和布局规则。
- 目的：把“机制是否成立”和“视觉包装是否强化玩家感知”分开验证，避免 AI 直接把规则实现成无感 UI，也避免生图效果图脱离可操作原型。
- 后续待制定：美术风格稿真源格式、视觉组件词典字段、效果图 prompt 模板、效果图拆解模板、真实截图对照验收方式。
- 记录边界：当前只记录工作流点子，不把本轮临时生图尝试或具体风格图本身写入 `docs/设计采纳记录.md`。

---

## 8. 建议后续 AI 的开场动作

当后续 AI 接手本仓库，尤其接到玩法、原型、交互、UI 感知类任务时，建议顺序：

1. 先读本文。
2. 若涉及 UI / 交互 / 原型呈现，再读 `docs/onboarding/ui-interaction-guidelines.md`。
3. 再读 `docs/onboarding/repository-map.md`。
4. 再确认当前任务讨论的是哪种载体：
   - `design/prototypes/html/`  
   - `gd_project/`  
5. 明确回答自己三个问题：
   - 用户要的是规则层，还是体验层？  
   - 用户要的是最小补丁，还是重做？  
   - 用户最早想在哪个区域 / 界面玩到它？

如果这三个问题没有答清楚，不要急着改。

---

## 9. 本文与其他文件的关系

- 本文是 **AI 协作与用户偏好约束**，不是玩法真源。  
- `ui-interaction-guidelines.md` 是 **UI / 交互执行规范**，承接本文中的 UI 错题，提供后续实现检查清单。
- 玩法真源仍以 `design/gdd/` 为准。  
- 若本文与 `AGENTS.md`、`.cursor/rules/` 有重叠，优先把最短、最硬的约束同步进规则文件；把分析细节留在本文。

---

**维护说明**：后续每次用户给出新的纠偏反馈，AI 都应更新本文，而不是把经验留在当前会话里遗失。  
