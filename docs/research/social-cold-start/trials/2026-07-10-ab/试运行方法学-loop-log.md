# 社媒模型A/B试运行方法学 Loop Log

结论：首轮原始扫描不能直接把召回差距归因于模型档位；保留首轮作为自主工具编排表现，并补跑固定批次和等待策略的默认组。

影响：默认组一次并发12条RedNote查询并在约190秒后整体终止，全Sol组则分3批并持续等待每批完成。两组同时改变了模型与工具等待策略，若直接评分会夸大全Sol的知识 / 推理优势。

下一步：固定3批、每批4条、每批最多等待240秒，用Luna / low复跑；随后由Terra / medium归纳，再分别报告自主执行差距和受控质量差距。

- 触发来源：父级自检 + 两名采集执行者运行复盘。
- 原始问题：试运行规格冻结了查询次数，却没有冻结并发批次、续接等待和终止阈值。
- 失败归因：实验控制与证据归因。
- 复发判定：新错误，不属于现有UI / 资产交付错误家族。
- 本轮处理：降级首轮因果结论，补做受控复跑。
- 复发保护：后续模型工具调用A/B必须同时冻结查询、批次、等待、重试、终止阈值和执行顺序； live MCP结果仍需交叉顺序或重复轮次。
- 沉淀判断：先留在本次试运行目录；若后续模型评测再次发生，再升级到通用研究评测规范。

