# 原始扫描

Luna 自动任务每周在此创建一份日期文件，命名为 YYYY-MM-DD.md。

每份扫描必须包含：

- 覆盖窗口和实际使用的平台入口。
- 搜索词组与新增实体词。
- 20-30条候选，或说明为什么不足。
- 高互动、中位、低表现和异常比例样本。
- 可见互动数据、采集时间、链接和证据等级。
- 与案例库的重复检查。
- 平台访问失败和待人工补看候选。

原始扫描只负责完整、诚实和可复查，不直接改写冷启动手册。

