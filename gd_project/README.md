# Godot 项目

`gd_project/` 是 Angus 正式的运行时开发根目录。

请用 Godot 4.3 打开 `project.godot`，并把该目录下的所有内容都视为项目自有运行时内容：

- `scenes/`：玩法、UI、开发场景，以及与场景绑定的脚本
- `Assets/`：运行时资产与项目使用的导入源美术
- `addons/`：项目本地 Godot 插件
- `export_presets.cfg`：构建自动化使用的导出配置

如果你要改游戏行为、UI 或运行时资产，先从这里开始。
