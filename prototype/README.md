# Prototype Packages

This directory is for distributable prototype bundles and curated export artifacts.

Current rule:

- Rebuildable package outputs are generated by scripts and should stay ignored by default
- Promote a generated package into tracked repository history only when there is a deliberate reason to preserve that exact build artifact

Current generated package path:

- `prototype/fullchain_demo/`
- `prototype/fullchain_demo.zip`

Current packaging script:

- `scripts/release/package_fullchain_demo.ps1`
