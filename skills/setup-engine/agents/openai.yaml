interface:
  display_name: "Setup Engine"
  short_description: "Configure the project's game engine and version. Pins the engine in project docs, detects knowledge gaps, and populates engine reference docs via WebSearch when the version is beyond the LLM's training data."
  default_prompt: "Use $setup-engine to run the Setup Engine workflow in this Codex project."
