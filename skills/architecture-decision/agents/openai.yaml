interface:
  display_name: "Architecture Decision"
  short_description: "Creates an Architecture Decision Record (ADR) documenting a significant technical decision, its context, alternatives considered, and consequences. Every major technical choice should have an ADR."
  default_prompt: "Use $architecture-decision to run the Architecture Decision workflow in this Codex project."
